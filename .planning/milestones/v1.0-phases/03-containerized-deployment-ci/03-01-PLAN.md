---
phase: 03-containerized-deployment-ci
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - providers/openai_compatible_base.py
  - Dockerfile
  - .dockerignore
  - docker-compose.yml
  - Makefile
  - README.md
autonomous: true
requirements:
  - DEPL-01
  - DEPL-02
user_setup:
  - service: rancher-desktop
    why: >-
      Docker daemon for the DEPL-01/DEPL-02 runtime acceptance (image build, compose up,
      healthcheck, volume persistence, recreation proof). RESEARCH verified the daemon socket
      absent locally while the docker CLI (29.5.3-rd), compose v5.1.4, and buildx are present;
      the locked post-research decision makes acceptance LOCAL with the operator starting
      Rancher Desktop. Claude runs every command once the daemon is up.

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'TR1 (DEPL-01 blocker, edge disposed: eager-vs-lazy annotation evaluation): providers/openai_compatible_base.py gains exactly one import line — the collections.abc spelling of AsyncGenerator, placed in the stdlib group immediately after import logging — and nothing else changes in the file; a real Python 3.12 environment with requirements.txt installed imports the providers package and passes the full suite with exit 0 (pre-fix, empirically reproduced: NameError naming AsyncGenerator, 2 collection errors, exit 2), while the 3.14 dev suite stays green — BOTH annotation regimes hold.'
    - 'TR2 (DEPL-01 image shape): docker build produces llm-gateway:latest from exactly two python:3.12-slim stages with byte-identical FROM tags (the venv-copy relocatability invariant — same interpreter path in both stages); the runtime stage runs USER gateway (docker inspect Config.User = gateway, uid non-zero at runtime), WORKDIR /app, and the exec-form CMD array launching uvicorn main:app bound to 0.0.0.0:8000 so uvicorn is PID 1 and receives SIGTERM directly; the HEALTHCHECK probes /health with the stdlib urllib one-liner (--interval=30s --timeout=5s --start-period=10s --retries=3).'
    - 'TR3 (DEPL-02 tracer, operator-gated): with Rancher Desktop running and a .env at the repo root, docker compose up builds the image and brings up the ONE gateway service to health=healthy; GET http://localhost:8000/health returns 200 with status ok and no auth (baseline exemption preserved).'
    - 'TR4 (DEPL-01 edge disposed — probe category unclassified, resolved as a measurement boundary per /tmp/gsd-edge-coverage-p3.json): in-container request-readiness is MEASURED, never assumed — elapsed wall time from the docker compose up -d invocation to the first 200 from /health, polled at <=100ms intervals, recorded verbatim in the SUMMARY evidence; the criterion is strictly under 3 seconds (NFR-04 in-container); any overshoot is escalated to the user as a criterion-vs-optimization decision (RESEARCH assumption A6, local cold start 3.21s), never silently absorbed and never preemptively optimized — no bytecode precompilation and no import trimming land in this phase.'
    - 'TR5 (DEPL-02 edge disposed — probe category concurrency, "If interrupted or run in parallel, what is guaranteed?"): docker compose down (plain, never the volume-removing flag) stops the container via SIGTERM -> uvicorn PID-1 graceful shutdown -> full lifespan drain (AnalyticsWriter drain bound 5s sits inside the 10s default stop grace — no extra compose knob; "Application shutdown complete." appears in the container logs); after down + up, every committed request_logs row survives on the gateway-data named volume (marker-row proof); the volume-removing down flag is the ONLY data-destroying edge and the README documents it as such; parallel container instances are NOT a supported posture (single service, single process — no replicas).'
    - 'TR6 (DEPL-02 config boundary, prohibition enforcement): no .env file exists inside any image layer — the runtime probe asserts absence at /app/.env — configuration crosses exactly one path: host .env -> compose env_file -> container env -> pydantic Settings (which finds no .env file at WORKDIR /app and reads the process env instead); the image COPY allowlist enumerates only the runtime modules main.py, config.py, rate_limiter.py, requirements.txt and the analytics/, routes/, providers/, static/ packages.'
    - 'TR7 (locked scope, dev-flow preservation): the Makefile dev flow is untouched — existing targets byte-identical, only the line-1 .PHONY list gains the three docker target names, and three new targets land under one comment header after clean; README gains exactly one new section — Deployment (Docker) — inserted between Architecture and Development; nothing else in either file changes.'
  artifacts:
    - providers/openai_compatible_base.py
    - Dockerfile
    - .dockerignore
    - docker-compose.yml
    - Makefile
    - README.md
  key_links:
    - 'config path: compose env_file .env -> container environment -> config.py Settings (model_config env_file .env finds no file at /app WORKDIR, resolves from process env) -> main.py lifespan APP_API_KEY fail-fast — the single configuration path; no environment: overrides exist anywhere'
    - 'secret/context exclusion: Dockerfile COPY allowlist + .dockerignore .env entry — the two independent layers keeping secrets and the ~49MB of repomix output out of image layers and build context (RESEARCH Pitfall 3)'
    - 'volume ownership: Dockerfile mkdir + chown gateway:gateway /app/data BEFORE USER -> named-volume copy-up preserves non-root ownership -> main.py lifespan mkdir + os.access write-check passes -> AnalyticsWriter starts (skipping the chown crash-loops the container on the lifespan not-writable RuntimeError — Pitfall 2)'
    - 'CWD invariant: WORKDIR /app <-> CWD-relative runtime paths (static/ mounts and FileResponse in main.py, default ANALYTICS_DB_PATH=data/analytics.db in config.py) — uvicorn must start with CWD=/app'
    - 'signal path: exec-form CMD -> uvicorn PID 1 -> SIGTERM on docker stop -> lifespan shutdown -> AnalyticsWriter.stop() drain (empirically verified exit-143 sequence) — 5s writer drain inside the 10s default stop grace'
    - 'operator path: make docker-up (compose up detached WITH build-always) -> compose build . -> image llm-gateway:latest (name shared with make docker-build) -> HEALTHCHECK -> GET /health green'
  prohibitions:
    - requirement_id: DEPL-01
      category: privacy
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: 'MUST NOT bake secrets into the image — no .env file, no API keys, and no dev analytics database may exist in any image layer; configuration reaches the process exclusively via the compose env_file -> container-env path at runtime; the runtime probe asserting /app/.env absence plus the COPY allowlist are the enforcement (specless-probe kept prohibition, descriptor-less / flagged-unverified).'
    - requirement_id: DEPL-01
      category: safety
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: 'MUST NOT introduce multi-process serving — no gunicorn, no uvicorn workers flag, no compose replicas or scale — the container stays single-process by design: the per-process AnalyticsWriter queue, single-writer SQLite connection, and per-worker slowapi counters make workers a correctness regression, not scaling (RESEARCH Alternatives table; specless-probe kept prohibition, descriptor-less / flagged-unverified).'
---

<objective>
Tracer slice for Phase 3: ONE end-to-end path — the Python-3.12-blocking AsyncGenerator import fix flowing into a multi-stage non-root image, a single-service compose stack, and a green /health on the operator-started Rancher Desktop daemon — then the DEPL-02 acceptance expansion (volume persistence across recreation, SIGTERM-drain evidence, measured <3s readiness) and the README deployment quickstart documenting exactly what was proven.

Implements the locked CONTEXT decisions "Container + Compose" (python:3.12-slim multi-stage venv-copy Dockerfile, non-root user, HEALTHCHECK /health; .dockerignore locked list; ONE compose service with env_file .env, port 8000, named volume for data/, restart unless-stopped; no profiles, no overrides) and "Local Workflow + Docs" (Makefile dev flow untouched + three docker targets; README short deployment quickstart, nothing else), plus the locked post-research decision that runtime acceptance runs LOCALLY with the operator starting Rancher Desktop. The decisions are LOCKED — this plan implements them exactly via the research-verified skeletons; it does not redesign them.

Mandatory sequencing fact (empirically reproduced in 03-RESEARCH Pitfall 1): the AsyncGenerator import fix is the FIRST task of the phase — without it the CI 3.12 matrix leg fails at pytest collection (exit 2) and the python:3.12-slim container crash-loops on startup. RESEARCH key insight guards scope: this phase is four config files plus one line of Python — if implementation starts writing application code beyond that one import line, scope has drifted.

Purpose: prove the phase's riskiest seams (3.12 eager-annotation crash; non-root volume ownership; compose env passthrough onto pydantic-settings) on the earliest commit, live on the real target runtime.

Output: a healthy single-container deployment of the gateway with persistent analytics, proven end-to-end and documented; existing dev flow and full test suite undisturbed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-containerized-deployment-ci/03-CONTEXT.md
@.planning/phases/03-containerized-deployment-ci/03-RESEARCH.md
@.planning/phases/03-containerized-deployment-ci/03-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end container tracer — AsyncGenerator 3.12 fix → multi-stage non-root image → compose up → green /health (one path only)</name>
  <files>providers/openai_compatible_base.py, Dockerfile, .dockerignore, docker-compose.yml, Makefile</files>
  <precondition>Rancher Desktop is running — `docker info` exits 0 (user_setup above; locked post-research decision). If unmet: halt this task's container stages and surface the user_setup item via checkpoint; the RED/GREEN Python-3.12 portion (steps 1-2) may still be executed and committed while waiting.</precondition>
  <read_first>
  - .planning/phases/03-containerized-deployment-ci/03-CONTEXT.md — all locked decision blocks (Container + Compose, Local Workflow + Docs) and the Post-Research Decisions (local acceptance with Rancher Desktop)
  - .planning/phases/03-containerized-deployment-ci/03-RESEARCH.md — §Code Examples (Dockerfile, docker-compose.yml, Makefile additions — verified skeletons), Patterns 1-4, Anti-Patterns to Avoid, Pitfalls 1, 2, 3, 4, 9, §Open Questions Q2 and Q3, Summary (SIGTERM verification)
  - .planning/phases/03-containerized-deployment-ci/03-PATTERNS.md — sections "Dockerfile", ".dockerignore", "docker-compose.yml", "Makefile" (skeletons quoted verbatim; the codebase-anchors list under Dockerfile is non-negotiable — do not "improve" the elements), section "providers/openai_compatible_base.py", and "Shared Patterns"
  - providers/openai_compatible_base.py lines 1-9 (current import block) and line 27 (the annotation)
  - providers/base.py lines 1-2 (the in-repo import-convention analog — and the PATTERNS note on why the collections.abc spelling is preferred over it)
  - Makefile (whole file, 47 lines — .PHONY line 1, clean target lines 44-47)
  - AGENTS.md — Runtime/Tooling Preferences (Python floor, pip+venv, port-8000 convention), Development Commands
  </read_first>
  <behavior>
  - RED (must be observed and recorded BEFORE the fix): in a real Python 3.12 venv with requirements.txt installed, `pytest -q` fails at collection — NameError naming AsyncGenerator, errors in tests/test_openai_compatible_base.py and tests/test_providers.py, "Interrupted: 2 errors during collection", exit code 2 — the empirically reproduced crasher (local 3.14 passes only because PEP 649 made annotations lazy).
  - GREEN: the same command exits 0 after the one-line import fix; the 3.14 dev suite (.venv) also exits 0 — both annotation regimes hold.
  - Container end: `docker compose up -d` builds the image and the gateway container reaches health=healthy; `curl http://localhost:8000/health` returns 200 with status ok.
  </behavior>
  <action>
  RED — build the standing 3.12 regression rig first: python3.12 is at /opt/homebrew/bin/python3.12 (3.12.14, verified). Create or reuse the venv at /tmp/gw312 (python3.12 -m venv /tmp/gw312), install requirements into it, run `/tmp/gw312/bin/python -m pytest tests/ -q` and record the exit-2 NameError output as the failing evidence in the SUMMARY (this rig is reused by 03-02 and by every future import-touching change; note the recreate command there).

  GREEN + the four config files, implementing the locked CONTEXT "Container + Compose" decisions exactly:

  1. THE FIX (mandatory first change — the whole phase is blocked without it): in providers/openai_compatible_base.py add exactly one import line to the stdlib group immediately after import logging (line 3): the collections.abc spelling of AsyncGenerator (modern form, research-verified working on 3.12; deliberately NOT the deprecated typing spelling used by providers/base.py — PATTERNS records the decision). No other change to this file. Re-run the 3.12 rig: exit 0.

  2. Dockerfile — author from the 03-PATTERNS §Dockerfile skeleton verbatim, keeping every load-bearing element: the syntax-directive comment line; builder stage FROM python:3.12-slim AS builder creating a venv at /opt/venv with pip install --no-cache-dir -r requirements.txt; runtime stage FROM python:3.12-slim with the BYTE-IDENTICAL tag (venv relocatability); the ENV trio (PYTHONDONTWRITEBYTECODE, PYTHONUNBUFFERED, venv PATH); WORKDIR /app; the single RUN creating the system group/user gateway AND mkdir -p /app/data AND chown gateway:gateway /app/data — strictly before USER (Pitfall 2: a fresh named volume copies up the mount-point content with its ownership; a root-owned dir crash-loops the container on the lifespan not-writable RuntimeError); COPY --from=builder /opt/venv /opt/venv; the COPY allowlist enumerating EXACTLY main.py, config.py, rate_limiter.py, requirements.txt into ./ plus the analytics, routes, providers, static directories — never a whole-context copy (Pattern 2: the allowlist beats any blocklist; .env and the ~49MB repomix files can never enter a layer even if .dockerignore regresses); USER gateway; EXPOSE 8000; the HEALTHCHECK directive with interval 30s, timeout 5s, start-period 10s, retries 3, running the stdlib urllib.request one-liner against http://127.0.0.1:8000/health (curl/wget are absent on slim; these exact command bytes were validated under python 3.12 against a live server and a dead port); and the exec-form CMD JSON array running uvicorn main:app with host 0.0.0.0 and port 8000 (the Makefile serving convention; exec form keeps uvicorn PID 1 so SIGTERM reaches it directly — Pattern 3; a shell-form CMD would break the verified graceful-drain path). Drop the bracketed research annotations ([CITED]/[VERIFIED]/[ASSUMED]) from file comments; keep the operational ones.

  3. .dockerignore — a flat newline-separated list, one entry per line, comments allowed: the locked CONTEXT entries exactly (.venv, .git, data/, .planning/, plans/, docs/, __pycache__, the two repomix artifact filenames, server.log), then the RESEARCH Pitfall-3 additions (.env — the single most important entry, secrets; .pytest_cache, .omx, .claude, .superpowers, .ngrok, the pycache-suffix pattern for compiled files, tests/). Author it independently of .gitignore — RESEARCH verified .gitignore does NOT cover server.log, the repomix files, or .omx, so it is not a substitute.

  4. docker-compose.yml — from the 03-PATTERNS skeleton verbatim: ONE service named gateway with build dot, image llm-gateway:latest (the stable name deliberately shared with make docker-build), env_file .env (required by default — a missing file errors loudly before start, Pitfall 6 fail-fast layer one), the published port mapping 8000:8000, the named volume gateway-data mounted at /app/data (the CWD-relative ANALYTICS_DB_PATH default), restart unless-stopped; top-level volumes declaring gateway-data. NO obsolete compose version key, NO environment: entries (env_file is the single source — an environment entry would override it), no profiles, no override files (locked YAGNI).

  5. Makefile — dev flow untouched: after the clean target (line 47) append a blank line, one comment header for the docker deployment block, and three targets — docker-build (docker build -t llm-gateway:latest .), docker-up, docker-down (docker compose down); extend the line-1 .PHONY list with the three names (single .PHONY line, never a second one — Makefile hygiene pattern). Planner decision on the choice RESEARCH explicitly left open (up-plain vs up-with-build): docker-up runs compose up detached WITH the build-always flag — it makes make docker-up self-sufficient on a fresh clone and removes the stale-image-after-edits footgun; this is one of the two sanctioned options and Task 2 documents it in the README.

  6. Runtime acceptance on the operator-started daemon (locked post-research decision): make docker-build — watch the transferring-context line (tens of MB means .dockerignore regressed; warning sign from Pitfall 3); docker compose up -d; poll http://localhost:8000/health until 200 (the curl retry pattern in the verify block); capture the docker compose ps health column and docker inspect Config.User. Diagnosis guide: a restarting container ending in the not-writable data-dir RuntimeError means the chown regressed (Pitfall 2); a NameError traceback means the fix regressed (Pitfall 1).

  Commit per AGENTS.md conventions (tracer commit: feat(03-01)). Drift guard: application-code changes beyond the one import line are out of scope — stop and reassess against the plan.
  </action>
  <verify>
    <automated>docker info >/dev/null && /tmp/gw312/bin/python -m pytest tests/ -q && .venv/bin/python -m pytest tests/ -q && docker compose up -d --build && curl -sf --retry 20 --retry-delay 1 --retry-connrefused http://localhost:8000/health && docker inspect --format '{{.Config.User}} {{.State.Health.Status}}' $(docker compose ps -q gateway)</automated>
    <fails_when>any stage exits non-zero — specifically: the 3.12 rig exits 2 with a collection NameError (fix missing or wrong spelling), the 3.14 suite reports failures (regression), curl exhausts its retries (container crash-looping — run docker compose logs gateway and look for the NameError traceback or the ANALYTICS_DB_PATH not-writable RuntimeError), or docker inspect prints an empty/root user or a health status other than healthy</fails_when>
  </verify>
  <acceptance_criteria>
  - The fix: grep -c "^from collections.abc import AsyncGenerator$" providers/openai_compatible_base.py returns 1, and the task's own commit diff for the file shows exactly 1 insertion and 0 deletions.
  - 3.12 regime: /tmp/gw312/bin/python -m pytest tests/ -q exits 0 (RED evidence — the pre-fix exit-2 NameError output — recorded in the SUMMARY).
  - 3.14 regime: .venv/bin/python -m pytest tests/ -q exits 0.
  - Image shape: grep -c "^FROM python:3.12-slim" Dockerfile returns 2 (byte-identical tags); grep -c "^USER gateway$" Dockerfile returns 1; grep -c "^CMD \[\"uvicorn\"" Dockerfile returns 1 (exec form only); grep -c "^HEALTHCHECK" Dockerfile returns 1; grep -c "^WORKDIR /app$" Dockerfile returns 1.
  - Copy discipline: grep -c "^COPY main.py config.py rate_limiter.py requirements.txt ./$" Dockerfile returns 1; grep -c "COPY . \." Dockerfile returns 0.
  - Non-root at runtime: docker inspect --format "{{.Config.User}}" $(docker compose ps -q gateway) prints gateway; docker run --rm llm-gateway:latest python -c "import os; raise SystemExit(0 if os.getuid() != 0 else 1)" exits 0.
  - No secrets baked (prohibition): docker run --rm llm-gateway:latest python -c "import os; raise SystemExit(1 if os.path.exists('/app/.env') else 0)" exits 0.
  - .dockerignore: grep -cE "^\.env$" .dockerignore returns 1; grep -vc "^#" .dockerignore reports at least 18 (10 locked entries + 8 additions); both repomix artifact filenames are present as entries.
  - Compose: grep -c "env_file: .env" docker-compose.yml returns 1; grep -c "gateway-data" docker-compose.yml returns 2 (mount + declaration); grep -c "^version:" docker-compose.yml returns 0.
  - Makefile: grep -cE "^docker-(build|up|down):" Makefile returns 3; the task's own commit diff shows exactly one removed pre-existing line — git diff -U0 Makefile > /tmp/p3-makefile.diff && [ "$(grep -c "^-[^-]" /tmp/p3-makefile.diff)" -eq 1 ] exits 0 at commit time (git's own failure fails the gate, never reads as clean); grep -c "up -d --build" Makefile returns 1.
  - Live: docker compose ps lists the gateway service with health healthy; curl -sf http://localhost:8000/health returns the status-ok body.
  </acceptance_criteria>
  <done>The one container path works end-to-end and is committed: the 3.12 blocker is fixed (both matrix regimes green), the image builds multi-stage and non-root with no secrets inside any layer, and a plain compose up on the operator-started Rancher Desktop daemon reaches a healthy /health.</done>
  <reversibility rating="reversible">One import line plus four additive config files and three Makefile targets — a single git revert restores current state; the only persistent side effects are operator-owned local Docker artifacts (the llm-gateway:latest image and the gateway-data volume), not repo state.</reversibility>
</task>

<task type="auto">
  <name>Task 2: DEPL-02 acceptance expansion — volume persistence across recreation, SIGTERM-drain evidence, measured &lt;3s readiness, README deployment quickstart</name>
  <files>README.md</files>
  <precondition>Task 1's compose stack is up and healthy on the Rancher Desktop daemon (docker compose ps shows healthy), and a .env with APP_API_KEY exists at the repo root (the operator's existing one — fresh-clone fidelity is the same build-from-checkout + env_file passthrough these commands exercise).</precondition>
  <read_first>
  - .planning/phases/03-containerized-deployment-ci/03-RESEARCH.md — §Pitfalls 5 (env dollar-sign interpolation), 8 (single-process temptation), 9 (NFR-04 timing untested until smoke); §Open Questions Q2 (measurement protocol) and Q3; §Code Examples "One-time VACUUM inside the container" (the container-sqlite one-liner pattern); Summary paragraph (the verified SIGTERM graceful-shutdown log sequence and exit 143)
  - .planning/phases/03-containerized-deployment-ci/03-PATTERNS.md — section "README.md" (placement between the Architecture section ending line 272 and the Development heading at line 274; the Quick Start subsection style; the command-block style; the do-not-touch note on Configuration tables) and section "Makefile" (the documented up-choice this section must state)
  - README.md lines 240-290 (Architecture tail, Development section and its target listing, License)
  - tests/test_analytics_db.py lines 212-236 (the explicit-columns raw-sqlite3 seeding pattern the marker row mirrors)
  - analytics/db.py request_logs schema columns (for the explicit-columns INSERT list)
  - AGENTS.md — Important Files (.env.example canonical env surface)
  </read_first>
  <action>
  1. Marker-row persistence proof (one compose session, all via docker compose exec with the container-sqlite pattern): seed exactly one marker row into the volume DB from inside the container — sqlite3 connecting data/analytics.db with an explicit-columns INSERT mirroring the test-suite seeding pattern (a distinctive marker id, probe provider/model values, zeros for the numeric columns, an ISO created_at — never a bare values-list without columns). Read back and record the row count.

  2. Interruption guarantee (TR5): run docker compose down with the plain form (never the volume-removing flag) and capture the gateway container logs BEFORE re-up — they must show the graceful sequence from the empirically verified drain (the shutting-down banner through application-shutdown-complete; writer drain bound 5s inside the 10s default grace). Bring the stack back with docker compose up -d and re-read the marker count via the same exec pattern — the identical count proves every committed row survived recreation (DEPL-02 volume persistence).

  3. Readiness measurement (RESEARCH Q2 protocol — the criterion is borderline-by-measurement, never assumed): with the stack down, timestamp immediately before docker compose up -d, poll http://localhost:8000/health at <=100ms intervals until the first 200, and record the elapsed figure VERBATIM in the SUMMARY evidence. Strictly under 3 seconds satisfies NFR-04 in-container; on any overshoot, record it and escalate to the user as a criterion-vs-optimization decision — do NOT land optimization work (no precompilation, no import trimming; RESEARCH explicitly defers those to a user decision).

  4. Probe cleanup: DELETE the marker row via the same exec pattern so the operator's analytics stays clean; record the before/after counts in the SUMMARY.

  5. README deployment quickstart (locked decision — short quickstart, nothing else): insert exactly one new level-2 section titled Deployment (Docker) between the Architecture section (ends line 272) and the Development heading (line 274), mirroring the README's own conventions (env pointer shaped like the Configure block; bash block shaped like the Run block; one-line target comments like the Development listing). Contents: the env pointer (copy .env.example to .env and edit APP_API_KEY — the one required variable); one bash block with the compose-up command including the build flag (matching make docker-up exactly); the three make target one-liners (docker-build, docker-up, docker-down); a named-volume note stating the analytics DB persists across container recreation on the gateway-data volume and that only the volume-removing down flag deletes it; one line noting env values containing a dollar sign must be single-quoted (compose interpolation pitfall 5); one line stating the container is single-process by design (pitfall 8). Do NOT touch the Configuration tables — the env surface is unchanged (vars pass through env_file to the same Settings).
  </action>
  <verify>
    <automated>docker compose exec gateway python -c "import sqlite3; c=sqlite3.connect('data/analytics.db'); print(c.execute('SELECT COUNT(*) FROM request_logs').fetchone()[0]); c.close()" && grep -c "^## Deployment (Docker)$" README.md && grep -c "docker compose up -d --build" README.md</automated>
    <fails_when>the count query errors or the DB path is missing inside the container (volume not mounted at /app/data — wiring broke), the README heading count is not exactly 1 (section missing, duplicated, or wrong level), or the quickstart compose command line is absent (docs do not match the Makefile target)</fails_when>
  </verify>
  <acceptance_criteria>
  - The SUMMARY records: the marker count before down EQUALS the count after up (recreation survival), the captured graceful-shutdown log line, the measured readiness figure with its strict <3s verdict (or the escalation), and the marker-cleanup confirmation.
  - grep -c "^## Deployment (Docker)$" README.md returns 1, and the section sits between the Architecture and Development headings.
  - grep -c "docker compose up -d --build" README.md returns 1; grep -cE "docker-build|docker-up|docker-down" README.md returns 3 (three one-line target entries).
  - README insertions only: the task's own commit diff shows zero removed lines — git diff -U0 README.md > /tmp/p3-readme.diff && [ "$(grep -c "^-[^-]" /tmp/p3-readme.diff)" -eq 0 ] exits 0 at commit time (a failing git fails the gate instead of reading as a clean zero) — Configuration tables and all other sections untouched.
  - Persistence is repeatable: one additional down/up cycle still returns the same committed-row count via the exec probe.
  </acceptance_criteria>
  <done>DEPL-02 proven live on the operator daemon and documented: committed rows survive container recreation on the named volume, SIGTERM drains gracefully inside the grace period, in-container readiness is a measured figure against the <3s criterion (escalations surfaced, not absorbed), and the README quickstart describes exactly the proven deployment flow.</done>
  <reversibility rating="reversible">README-only file change plus read-only runtime probes (the marker row is deleted; the local image and volume are operator-owned Docker state) — trivially revertible with no repo side effects.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host .env → compose env_file → container env → Settings | operator-controlled secrets cross one boundary into the container process; the only config ingress |
| build context → image layers | repo content becomes the shipped artifact — whatever enters the context can enter a layer |
| container (non-root) → named volume | the gateway process writes its analytics DB to persistent host-backed storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | secrets baked into image layers (Dockerfile COPY / build context) | high | mitigate | COPY allowlist (enumerated runtime modules only) + .dockerignore .env entry — two independent layers; runtime probe asserts no .env at /app (Task 1 acceptance, prohibition 1) |
| T-03-02 | Elevation of Privilege | container running as root / privileged | high | mitigate | USER gateway system user with no shell login, no added capabilities, no privileged flags; runtime uid probe asserts non-zero (Task 1 acceptance) |
| T-03-03 | Tampering | base-image supply chain (python:3.12-slim tag) | medium | mitigate | Official library image, tag active and patched 2026-09-02 (verified via Hub API this session); current digest recorded in RESEARCH if pinning is ever wanted |
| T-03-04 | DoS | root-owned named volume → lifespan not-writable abort → restart loop (Pitfall 2) | medium | mitigate | mkdir + chown gateway:gateway /app/data in the image strictly before USER; copy-up preserves ownership; the compose acceptance catches a regression immediately |
| T-03-05 | Information Disclosure | healthcheck endpoint as recon surface | low | accept | /health returns only the status-ok body (baseline, unchanged); unauthenticated by design so the container probe needs no credentials |

No package installs land in this plan (no new runtime dependencies; the image installs only the existing requirements.txt) — the ruff supply-chain row lives in 03-02 with its blocking human checkpoint.
</threat_model>

<verification>
- Both Python regimes green: /tmp/gw312/bin/python -m pytest tests/ -q and .venv/bin/python -m pytest tests/ -q both exit 0 (the 3.12 rig is the permanent regression test for the annotation blocker).
- Live container: docker compose ps reports the gateway service healthy; curl -sf http://localhost:8000/health returns the status-ok body; docker inspect Config.User prints gateway.
- Image hygiene: the no-.env runtime probe and the non-root uid probe both exit 0; the Dockerfile gates in Task 1 acceptance all pass (two byte-identical FROM tags, exec-form CMD, HEALTHCHECK present, allowlist-only COPY).
- Persistence: marker-row count identical across a down/up cycle; graceful-shutdown log line captured during the down.
- Readiness: measured figure recorded in the SUMMARY against the strict <3s criterion, with any overshoot escalated rather than absorbed.
- Dev-flow preservation: the only pre-existing line changed anywhere in the Makefile is the .PHONY line; README changes are insertions only.
</verification>

<success_criteria>
- DEPL-01: the image is multi-stage (two python:3.12-slim stages), runs as non-root gateway with a chown'd /app/data, and reaches request-readiness in a MEASURED time under 3s from compose-up (or the overshoot is escalated per A6).
- DEPL-02: compose up on the operator-started daemon with a .env reaches a green healthcheck; the analytics DB persists across container recreation on the gateway-data volume; all config crosses via the single env_file passthrough with no secrets in any layer (probed).
- The Python-3.12 blocker is gone in both regimes; the existing dev flow, env surface, and full test suite are undisturbed; the README documents the proven deployment.
</success_criteria>

## Artifacts this phase produces

Union inventory across the phase (03-02 extends it; the union is authoritative):

- providers/openai_compatible_base.py — one added import line (the collections.abc spelling of AsyncGenerator) in the stdlib group; nothing else.
- Dockerfile (NEW) — venv-copy multi-stage on python:3.12-slim (byte-identical tags); system user gateway; chown'd /app/data; COPY allowlist (main.py, config.py, rate_limiter.py, requirements.txt, analytics/, routes/, providers/, static/); USER gateway; EXPOSE 8000; urllib HEALTHCHECK (--start-period=10s); exec-form CMD uvicorn on 0.0.0.0:8000.
- .dockerignore (NEW) — the 10 locked entries + 8 research additions (.env first among them); flat list.
- docker-compose.yml (NEW) — one gateway service (build dot, image llm-gateway:latest, env_file .env, port 8000:8000, volume gateway-data:/app/data, restart unless-stopped); no version key, no environment entries, no profiles/overrides.
- Makefile — .PHONY extended; three targets (docker-build, docker-up with build-always flag, docker-down) under one comment header; dev flow byte-identical.
- README.md — one Deployment (Docker) section between Architecture and Development (env pointer, compose-up command, make targets, volume/dollar-sign/single-process notes).
- 03-02 adds: .github/workflows/ci.yml (lint + test matrix 3.12/3.14 + docker-build smoke), main.py import-line fix, analytics/__init__.py emptied to a 0-byte package marker.

<output>
Create `.planning/phases/03-containerized-deployment-ci/03-01-SUMMARY.md` when done
</output>
