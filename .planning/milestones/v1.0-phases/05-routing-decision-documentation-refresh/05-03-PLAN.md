---
phase: 05-routing-decision-documentation-refresh
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/deployment-guide.md
  - docs/code-standards.md
autonomous: true
requirements: [DOCS-02]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "docs/deployment-guide.md's Environment Variables Reference lists exactly the real config.py Settings fields (APP_API_KEY through ANALYTICS_RETENTION_DAYS), with zero stale per-provider key/base-URL tables and zero references to the non-existent LLM_PROVIDER/LLM_MODEL/LLM_BASE_URL legacy vars"
    - "docs/deployment-guide.md documents the real Docker/Compose deployment (Makefile targets, gateway-data volume, /health/ready HEALTHCHECK, optional observability profile) in place of the old 'Docker (Future)' placeholder"
    - "docs/code-standards.md's file tree and 'Adding a New Provider' recipe match the shipped 4-file provider layout (base.py, openai_compatible_base.py, manifest.py, zai_coding.py) with no MODEL_PRICING step"
    - "docs/code-standards.md's Error Handling and Testing Guidelines sections match the real nested SSE error-frame shape and the real ASGITransport + real-token test auth pattern"
  artifacts:
    - path: "docs/deployment-guide.md"
      provides: "accurate Docker/Compose deployment guide and current env surface"
      contains: "docker compose --profile observability"
    - path: "docs/code-standards.md"
      provides: "accurate file tree, provider recipe, error/testing sections"
      contains: "zai_coding.py"
  key_links:
    - from: "docs/deployment-guide.md"
      to: "docker-compose.yml"
      via: "documented gateway-data volume and observability profile match the real Compose file"
      pattern: "gateway-data"
    - from: "docs/code-standards.md"
      to: "providers/__init__.py"
      via: "corrected factory code example matches the real if/else dispatch, not a match statement"
      pattern: "zai-coding"
  edge_cases:
    - category: concurrency
      status: resolved
      verification: explicit
      resolution: "This plan's two files (deployment-guide.md, code-standards.md) and the sibling 05-01/05-02 plans' files (PROJECT.md, tests/test_routing.py, project-overview-pdr.md, project-roadmap.md) have zero overlap - all three Phase 5 plans execute in wave 1 with disjoint files_modified, so parallel or interrupted execution of any one plan cannot corrupt another's file. Each doc edit here is a single atomic per-file write, git-tracked and idempotent to re-run."
  prohibitions:
    - statement: "MUST NOT modify AGENTS.md or REQUIREMENTS.md - explicitly out of scope for Phase 5 per 05-CONTEXT.md Deferred Ideas"
      status: resolved
      verification: test
---

<objective>
Bring `docs/deployment-guide.md` and `docs/code-standards.md` current with the shipped Docker/Compose deployment (Phase 3), the real env surface (`config.py`'s `Settings` class), and the real 4-file provider layout - removing dead per-provider key/base-URL tables, a false "Legacy Settings still supported" claim, a placeholder "Docker (Future)" section, a stale kebab-case-filenames claim, a removed `MODEL_PRICING` step, and stale error-handling/testing claims.

Purpose: DOCS-02's ROADMAP success criteria require deployment-guide.md to document "the Docker/Compose deployment and the current env surface... no stale per-provider key/base-URL tables" and code-standards.md's "file tree and 'Adding a New Provider' recipe match the shipped providers... no pricing-table step".
Output: corrected `docs/deployment-guide.md` (env-var reference, Docker section, troubleshooting) and `docs/code-standards.md` (naming table, file tree, provider recipe, error handling, testing guidelines).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-routing-decision-documentation-refresh/05-CONTEXT.md
@README.md
@AGENTS.md
@config.py
@providers/__init__.py
@routes/chat.py
@docker-compose.yml
@Dockerfile
@Makefile
@.env.example
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Correct docs/deployment-guide.md's env surface and Docker section</name>
  <read_first>docs/deployment-guide.md (full, 213 lines); config.py (full, 98 lines - the real 12-field Settings class); docker-compose.yml, Dockerfile, Makefile (full - real Docker/Compose facts)</read_first>
  <files>docs/deployment-guide.md</files>
  <action>
    Six corrections, all within docs/deployment-guide.md:

    1. Environment Variables Reference (lines 50-90): Replace the entire "Core Settings" + "Provider API Keys" + "Legacy Settings" + "Default Models by Provider" set of subsections with one consolidated table listing every real config.py Settings field: APP_API_KEY (required, no default), MANIFEST_API_KEY (optional, no default), ZAI_CODING_API_KEY (optional; unset keeps GLM on Manifest), ZAI_CREDITS_5H (default 28000), ZAI_CREDITS_WEEK (default 140000), LLM_API_KEY (fallback for MANIFEST_API_KEY/ZAI_CODING_API_KEY), CORS_ORIGINS (default empty), RATE_LIMIT (default 60/minute, per-IP), RATE_LIMIT_PER_KEY (default 60/minute, per-Bearer-token, applies independently alongside RATE_LIMIT), ANALYTICS_DB_PATH (default data/analytics.db), ANALYTICS_QUEUE_SIZE (default 1000), ANALYTICS_RETENTION_DAYS (default 90, 0 = keep forever).

    2. Delete the "Legacy Settings (still supported)" section entirely (the LLM_PROVIDER/LLM_MODEL/LLM_BASE_URL table) - config.py's Settings class defines no such fields; pydantic's extra="ignore" silently drops them if set, so the "still supported" claim is false.

    3. "Production Deployment > Direct Deployment" bash block: replace `export OPENAI_API_KEY=$(cat /run/secrets/openai_key)` and `export DEEPSEEK_API_KEY=$(cat /run/secrets/deepseek_key)` with `export MANIFEST_API_KEY=$(cat /run/secrets/manifest_key)` and `export ZAI_CODING_API_KEY=$(cat /run/secrets/zai_coding_key)`.

    4. "Recommended Production Settings" bullet: change "Use GET /health for load balancer health checks" to "Use GET /health/live for liveness and GET /health/ready for readiness/load-balancer health checks (the old single /health endpoint was removed with no back-compat alias)".

    5. Replace the entire "## Docker (Future)" section (the placeholder Dockerfile snippet plus "Not yet implemented") with a "## Docker / Compose Deployment" section documenting the real shipped setup: make docker-build (docker build -t llm-gateway:latest .) and make docker-up (docker compose up -d --build) / make docker-down; the image is a multi-stage python:3.12-slim build running as a non-root gateway user; docker-compose.yml mounts a named gateway-data volume at /app/data (survives docker compose down, not down -v) and passes config via env_file: .env; the container's HEALTHCHECK polls GET /health/ready every 30s; an optional Prometheus sidecar starts via docker compose --profile observability up (reachable at http://localhost:9090, scraping /metrics per prometheus/prometheus.yml).

    6. Troubleshooting "### Provider errors in SSE stream" section: replace "Verify provider-specific API key is set (e.g., OPENAI_API_KEY, DEEPSEEK_API_KEY)" and "If using Gemini/MiniMax: set LLM_API_KEY; for GLM: set GLM_API_KEY" with "Verify MANIFEST_API_KEY (or LLM_API_KEY fallback) is set for non-GLM models, and ZAI_CODING_API_KEY (or LLM_API_KEY fallback) is set for glm-* models to route to the z.ai coding endpoint - without an effective z.ai key, GLM models silently degrade to Manifest with canonical ids (no error)."
  </action>
  <verify>
    <automated>grep -c 'OPENAI_API_KEY\|DEEPSEEK_API_KEY\|MOONSHOT_API_KEY\|BYTEDANCE_API_KEY\|GLM_API_KEY\|LLM_PROVIDER\|LLM_MODEL\|LLM_BASE_URL' docs/deployment-guide.md; grep -c 'ZAI_CODING_API_KEY' docs/deployment-guide.md; grep -c 'docker compose --profile observability' docs/deployment-guide.md; grep -c 'Docker (Future)' docs/deployment-guide.md; grep -c '/health/ready' docs/deployment-guide.md</automated>
    <fails_when>any of the 8 stale env-var names remain (count above 0), the observability Compose profile is undocumented (0), the "Docker (Future)" placeholder heading still exists (count above 0), or no /health/ready reference is present (0).</fails_when>
  </verify>
  <acceptance_criteria>
    - Environment Variables Reference lists exactly the 12 real Settings fields (APP_API_KEY through ANALYTICS_RETENTION_DAYS) - no dead provider-key/base-URL tables.
    - Docker section documents the real Dockerfile/docker-compose.yml/Makefile targets, the gateway-data volume, and the /health/ready HEALTHCHECK.
    - Troubleshooting section references the correct MANIFEST_API_KEY/ZAI_CODING_API_KEY key names.
  </acceptance_criteria>
  <done>deployment-guide.md documents the actual Docker/Compose deployment and the real 12-variable env surface with zero stale per-provider key or base-URL tables.</done>
</task>

<task type="auto">
  <name>Task 2: Correct docs/code-standards.md's naming, file tree, provider recipe, error handling, and testing sections</name>
  <read_first>docs/code-standards.md (full, 166 lines); AGENTS.md:94-262 (Key Directories, conventions, testing); providers/__init__.py (full, 15 lines); routes/chat.py:1-149 (error frame shape, factory usage)</read_first>
  <files>docs/code-standards.md</files>
  <action>
    Six corrections, all within docs/code-standards.md:

    1. Naming Conventions table: fix the "Python files" row from "kebab-case | providers/openai-provider.py, analytics/routing.py" to "snake_case | providers/manifest.py, providers/zai_coding.py, analytics/routing.py" (AGENTS.md explicitly flags the kebab-case claim as stale - every real file uses snake_case; do not touch the "Route prefixes | kebab-case" row, which is still accurate). Fix "Environment variables" row's examples from "OPENAI_API_KEY, DEEPSEEK_API_KEY, ANALYTICS_DB_PATH" to "MANIFEST_API_KEY, ZAI_CODING_API_KEY, ANALYTICS_DB_PATH". Fix "Constants" row's examples from "_ROLE_MAP, MODEL_ROUTING, MODEL_PRICING" to "_ROLE_MAP, MODEL_ROUTING, GLM_CANONICAL" (MODEL_PRICING no longer exists - cost calc was stubbed to 0.0 in the 2026-04-19 cutover).

    2. File Organization tree: replace the providers/ block (gemini.py/openai_provider.py/deepseek.py/moonshot.py/bytedance.py/glm.py/minimax.py) with the actual 4 files: base.py (ABC LLMProvider + UsageData/GenParams TypedDicts), openai_compatible_base.py (shared OpenAI-wire streaming impl), manifest.py (ManifestProvider, 6 lines), zai_coding.py (ZAICodingProvider, ~10 lines). Replace the analytics/cost.py tree comment "calculate_cost(), MODEL_PRICING" with "calculate_cost() (stub, returns 0.0), estimate_credits() (z.ai credit estimation)". Replace the tests/ file list: run `ls tests/*.py` (or an equivalent glob) LIVE at execution time and list every file it returns, alphabetically, one per line, in the tree — do NOT copy any fixed file list from this plan text (the suite has grown past any count recorded when this plan was authored; hardcoding would ship the doc freshly stale again).

    3. "Rules" bullet list: change "One provider per file in providers/" to note "- currently 4 files: base.py (ABC), openai_compatible_base.py (shared impl), manifest.py, zai_coding.py".

    4. "Adding a New Provider" recipe: Step 2 "Register in factory" - the actual providers/__init__.py factory is `if provider_name == "zai-coding": ... else: ManifestProvider` (a plain if/else, NOT a match statement) - correct the code example accordingly (an elif/else branch, not a case block). Delete Step 4 "Add pricing" entirely (MODEL_PRICING was removed 2026-04-19; calculate_cost() is now a stub always returning 0.0) and renumber the remaining steps (Step 5 "Add per-provider API key" becomes Step 4, Step 6 "Test" becomes Step 5).

    5. "Error Handling" section: remove the bullet "Unknown model: ValueError from resolve_provider() -- caught, returns 400" - resolve_provider() never raises; unknown non-GLM models passthrough to Manifest as-is and unknown glm-* models passthrough to zai-coding under the key gate (no 400 exists for this case). Remove the bullet "Unknown provider: ValueError from create_provider() -- returns 500" - create_provider() never raises either (its if/else always returns a provider). Fix the "Pattern for provider errors" code example: the actual error frame is a nested OpenAI-style object `{"error": {"message": "...", "type": "..."[, "code": "..."]}}` (built in routes/chat.py's _tracked_stream()), not a flat `{"error": error_msg}` string; the actual analytics logging call is `analytics_writer.enqueue({...})` (fire-and-forget on the bounded queue), not `asyncio.create_task(db.log_request({...}))`.

    6. "Testing Guidelines" section: fix "Tests use httpx AsyncClient with FastAPI's TestClient transport" to "Tests use httpx AsyncClient over ASGITransport (not FastAPI's sync TestClient)". Fix "Auth bypassed in tests via dependency override" - there is no app.dependency_overrides usage anywhere; tests instead supply a real matching Bearer token via the auth_headers() fixture ({"Authorization": "Bearer changeme"}, matching settings.app_api_key). Keep "Analytics DB uses in-memory SQLite for test isolation" (still true). Replace the "Test structure" file list with the SAME live `ls tests/*.py` enumeration used for item 2 above (re-run the glob for this section rather than reusing a cached list, in case the two edits land at slightly different moments).
  </action>
  <verify>
    <automated>grep -c 'openai-provider.py' docs/code-standards.md; grep -c 'providers/manifest.py' docs/code-standards.md; grep -c 'MODEL_PRICING' docs/code-standards.md; grep -c 'zai_coding.py' docs/code-standards.md; grep -c 'ASGITransport' docs/code-standards.md; grep -c 'dependency override' docs/code-standards.md; test "$(grep -oE '(test_[A-Za-z_]+|conftest)\.py' docs/code-standards.md | sort -u | wc -l)" = "$(ls tests/*.py | xargs -n1 basename | sort -u | wc -l)" && echo TESTLIST_MATCH || echo TESTLIST_MISMATCH</automated>
    <fails_when>the stale "openai-provider.py" filename remains (count above 0), "providers/manifest.py" is absent from the naming table example (0), "MODEL_PRICING" remains anywhere (count above 0), "zai_coding.py" is absent from the file tree (0), "ASGITransport" is absent from the Testing Guidelines section (0), the stale "dependency override" auth-bypass claim remains (count above 0), or the final check prints TESTLIST_MISMATCH (the doc's enumerated test-file count no longer matches the live count of tests/*.py).</fails_when>
  </verify>
  <acceptance_criteria>
    - Naming Conventions table's Python-files row shows snake_case with real file examples (manifest.py, zai_coding.py) - no kebab-case Python filename claim.
    - File Organization tree shows the actual 4 provider files (base.py, openai_compatible_base.py, manifest.py, zai_coding.py) and every file returned by a live `ls tests/*.py` enumeration at execution time (no hardcoded count).
    - "Adding a New Provider" recipe has no pricing step and the factory code example matches the real if/else shape.
    - Error Handling section's unknown-model/unknown-provider 400/500 claims are removed; the error-frame example matches the real nested {"error": {"message","type"[,"code"]}} shape.
    - Testing Guidelines correctly describe ASGITransport and real-token auth (no dependency-override claim); its "Test structure" list matches the same live enumeration as the File Organization tree.
  </acceptance_criteria>
  <done>code-standards.md's naming table, file tree, provider recipe, error handling, and testing sections all match the shipped codebase exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Static documentation content only, no runtime surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03-01 | Information (a stale deployment guide could lead an operator to misconfigure secrets - e.g. exporting OPENAI_API_KEY, which silently does nothing since config.py never reads it) | docs/deployment-guide.md | medium | mitigate | This plan's corrected env-var table and Docker section eliminate the stale/dead variable names and the false "still supported" legacy claim |

</threat_model>

<verification>
The Task 1 five grep checks and Task 2 seven checks (six grep + one live-count comparison) all pass on the corrected files.
</verification>

<success_criteria>
- docs/deployment-guide.md documents the real Docker/Compose deployment and the real 12-variable env surface, with zero stale per-provider key/base-URL tables.
- docs/code-standards.md's file tree, provider recipe, error handling, and testing sections all match the shipped codebase.
</success_criteria>

<output>
Create `.planning/phases/05-routing-decision-documentation-refresh/05-03-SUMMARY.md` when done.

## Artifacts Produced
- `docs/deployment-guide.md` - accurate 12-variable env surface, real Docker/Compose section, corrected troubleshooting
- `docs/code-standards.md` - accurate naming table, 4-provider file tree, live-enumerated test file list (matches `tests/*.py` at execution time), corrected provider recipe (no pricing step), corrected error-handling and testing-guidelines sections
</output>