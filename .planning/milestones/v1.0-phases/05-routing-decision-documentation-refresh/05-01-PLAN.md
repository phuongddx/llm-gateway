---
phase: 05-routing-decision-documentation-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_routing.py
  - .planning/PROJECT.md
autonomous: true
requirements: [ROUT-01]

estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "resolve_provider(\"auto\") == (\"manifest\", \"auto\") continues to hold, proven by two named regression tests covering both the with-zai-key and without-zai-key states"
    - "PROJECT.md records exactly one locked, dated ROUT-01 decision block (KEEP model=\"auto\" unchanged) plus a Key Decisions table row, matching the existing ZAI-1..4 block shape"
  artifacts:
    - path: "tests/test_routing.py"
      provides: "ROUT-01 regression coverage for model=\"auto\""
      contains: "test_resolve_auto_unaffected_by_zai_key_state"
    - path: ".planning/PROJECT.md"
      provides: "locked ROUT-01 decision record"
      contains: "id=\"ROUT-01\""
  key_links:
    - from: ".planning/PROJECT.md"
      to: "analytics/routing.py"
      via: "ROUT-01 decision text cites MODEL_ROUTING[\"auto\"] = (\"manifest\", \"auto\") as the unchanged entry"
      pattern: "auto"
  edge_cases:
    - category: idempotency
      status: resolved
      verification: explicit
      resolution: "Task 2's verify asserts exactly one `id=\"ROUT-01\"` block in PROJECT.md — a re-run of this plan must not duplicate the decision entry. Task 1 replaces (not appends to) the single prior test_resolve_auto with two named tests, so re-running the plan is idempotent rather than additive."
  prohibitions:
    - statement: "MUST NOT modify AGENTS.md or REQUIREMENTS.md — explicitly out of scope for Phase 5 per 05-CONTEXT.md Deferred Ideas"
      status: resolved
      verification: test
---

<objective>
Close the ROUT-01 routing decision: `model="auto"` is KEPT unchanged in `MODEL_ROUTING` — Manifest's 2026-09-01 deprecation removed the prompt-complexity classifier, not the `auto` keyword itself. This plan adds no routing code change; it adds a regression test proving the unchanged behavior and records the decision, dated and locked, in PROJECT.md's Key Decisions.

Purpose: ROUT-01's ROADMAP success criterion requires the decision be "decided with the user, recorded in PROJECT.md Key Decisions, and implemented with routing behavior + tests matching the recorded decision" — the decision is already locked in 05-CONTEXT.md (KEEP); this plan is the record + regression-test half of that criterion.
Output: two named regression tests in `tests/test_routing.py`; one locked, dated ROUT-01 decision block + Key Decisions table row in `.planning/PROJECT.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-routing-decision-documentation-refresh/05-CONTEXT.md
@analytics/routing.py
@tests/test_routing.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: ROUT-01 regression tests — `auto` stays pinned to Manifest</name>
  <read_first>tests/test_routing.py (full, 86 lines — `with_zai_key`/`without_zai_key` fixtures, existing `test_resolve_auto` at lines 85-86); analytics/routing.py (full, 81 lines — `MODEL_ROUTING["auto"] = ("manifest", "auto")`, `resolve_provider()`)</read_first>
  <files>tests/test_routing.py</files>
  <action>
    Replace the existing `test_resolve_auto(without_zai_key)` test (lines 85-86) with two named regression tests:

    1. `test_resolve_auto_unaffected_by_zai_key_state(without_zai_key)` — asserts `resolve_provider("auto") == ("manifest", "auto")`. Give it a docstring citing the ROUT-01 decision: Manifest's 2026-09-01 deprecation removed the prompt-complexity classifier specifically, not the `auto` keyword — `auto` now routes to the Manifest-dashboard-configured Default tier (one model + up to 5 fallbacks) with automatic fallback recovery, verified against current Manifest docs 2026-09-08. Reference `PROJECT.md` decision ROUT-01.

    2. `test_resolve_auto_stays_manifest_even_with_zai_key(with_zai_key)` — asserts the same `resolve_provider("auto") == ("manifest", "auto")`. Give it a docstring explaining this proves the effective-zai-key gate (which only branches `zai-coding`-provider `MODEL_ROUTING` entries inside `resolve_provider()`) never flips `auto`'s provider, since `auto`'s table entry is `("manifest", "auto")`, not a `zai-coding` entry — the key-gate branch is structurally unreachable for `auto`.

    Do not modify `MODEL_ROUTING` or `resolve_provider()` in `analytics/routing.py` — this is a regression test only, no routing code change.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_routing.py -k "test_resolve_auto" -v</automated>
    <fails_when>fewer than 2 tests are collected/passed for the `test_resolve_auto` selector, or either assertion fails (i.e. `resolve_provider("auto")` ever returns a tuple other than `("manifest", "auto")` under either key-state fixture).</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_resolve_auto" tests/test_routing.py` is >= 2 (both new tests present) and the old single `test_resolve_auto(without_zai_key):` signature line is absent (replaced, not duplicated).
    - Both new tests carry a docstring referencing the ROUT-01 decision rationale.
    - `analytics/routing.py` is unmodified (no `<files>` entry for it in this task).
  </acceptance_criteria>
  <done>Two named regression tests exist in tests/test_routing.py and both pass, proving `resolve_provider("auto")` is unaffected by z.ai key state.</done>
</task>

<task type="auto">
  <name>Task 2: Record the ROUT-01 decision in PROJECT.md</name>
  <read_first>.planning/PROJECT.md:13-52 (Locked Decisions intro + `<decisions>` block ZAI-1..4 + trailing "Also locked alongside" paragraph naming ROUT-01 as the future sanctioned path); .planning/PROJECT.md:121-133 (Key Decisions table, 8 rows + closing `---`)</read_first>
  <files>.planning/PROJECT.md</files>
  <action>
    Three edits, all within `.planning/PROJECT.md`:

    a) Immediately after the existing ZAI-1..4 `</decisions>` closing tag (end of that block) and before the "Also locked alongside" paragraph, add one new `<decisions>` block with attributes `locked="true" source="phase-5 research, verified against current Manifest docs" locked-with-user="2026-09-08" ingested="2026-09-08"`, containing a single `<decision id="ROUT-01" title="model=auto fate: KEEP unchanged">` element. Body content: KEEP `model="auto"` in `MODEL_ROUTING` exactly as-is (`("manifest", "auto")`) — no routing code change. Manifest's 2026-09-01 deprecation removed the prompt-complexity classifier specifically, not the `auto` keyword: `auto` now routes to the Manifest-dashboard-configured Default tier (one model + up to 5 fallbacks) with automatic fallback recovery on any 4xx/5xx from the primary — a different mechanism (dashboard-tier routing vs. prompt-based classification) than before, adding multi-model fallback `auto` traffic didn't have previously. Verified against current Manifest docs, 2026-09-08. Regression-proofed by `tests/test_routing.py::test_resolve_auto_unaffected_by_zai_key_state` and `::test_resolve_auto_stays_manifest_even_with_zai_key`.

    b) In the "Also locked alongside" paragraph's final sentence (currently ending "...that future decision is now roadmap item ROUT-01 (Phase 5), which is the sanctioned path to revisit it."), append a clause noting resolution: "— resolved 2026-09-08: KEEP, see the ROUT-01 decision block above."

    c) Add one row to the end of the "Key Decisions" table (after the "Playground: static HTML..." row, before the closing `---`): `| \`model="auto"\` kept unchanged (ROUT-01) | Verified against current Manifest docs — \`auto\` now rides the Default tier + up to 5 fallbacks, not the deprecated prompt-complexity classifier | ✓ Good |`
  </action>
  <verify>
    <automated>grep -c 'id="ROUT-01"' .planning/PROJECT.md; grep -c 'ROUT-01' .planning/PROJECT.md</automated>
    <fails_when>the `id="ROUT-01"` count is not exactly 1 (missing or duplicated decision block), or total `ROUT-01` mentions across the file are fewer than 3 (decision block + intro-paragraph clause + Key Decisions table row all required).</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly one `<decision id="ROUT-01" ...>` block exists, dated 2026-09-08, naming both regression tests from Task 1.
    - Key Decisions table has a `ROUT-01` row with a "✓ Good" outcome.
    - Repo-root `AGENTS.md` and `.planning/REQUIREMENTS.md` are unmodified.
  </acceptance_criteria>
  <done>PROJECT.md records the KEEP decision, locked and dated, cross-referencing the Task 1 regression tests, matching the ZAI-1..4 block shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan adds a regression test and a decision record only — no runtime code path, request handler, or trust boundary changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01-01 | Tampering (a future contributor edits or removes the locked decision without renegotiating with the user) | .planning/PROJECT.md decision block | low | accept | The `locked="true"` attribute + dated `locked-with-user` field mark the block as requiring explicit renegotiation, matching the existing ZAI-1..4 convention — no code enforcement needed for a planning-doc convention |

</threat_model>

<verification>
`.venv/bin/python -m pytest tests/test_routing.py -k "test_resolve_auto" -v` shows 2 passed; the two PROJECT.md grep checks in Task 2 both pass.
</verification>

<success_criteria>
- `resolve_provider("auto")` is proven unchanged (`("manifest", "auto")`) under both z.ai key states via two named regression tests.
- PROJECT.md records exactly one locked, dated ROUT-01 decision plus a Key Decisions table row.
</success_criteria>

<output>
Create `.planning/phases/05-routing-decision-documentation-refresh/05-01-SUMMARY.md` when done.

## Artifacts Produced
- `tests/test_routing.py` — two named ROUT-01 regression tests replacing the prior single `test_resolve_auto`
- `.planning/PROJECT.md` — locked, dated ROUT-01 decision block + Key Decisions table row
</output>
