---
phase: 05-routing-decision-documentation-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/project-overview-pdr.md
  - docs/project-roadmap.md
autonomous: true
requirements: [DOCS-01]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "docs/project-overview-pdr.md's Product and Goals sections describe exactly two providers (Manifest, z.ai GLM Coding Plan), not the native 8-provider architecture"
    - "docs/project-overview-pdr.md's FR-7..FR-13 and FR-19 rows (8 total) are marked Superseded/historical, not deleted — all 22 original FR rows remain present for traceability to REQUIREMENTS.md's Historical section"
    - "docs/project-roadmap.md's Phase 3 (Docker/CI/observability/rate-limiting/retry) is marked Complete/Delivered, matching the actually-shipped work; only the 3 Low-priority v2-deferred items (secrets management, horizontal scaling, API versioning) remain Not started"
  artifacts:
    - path: "docs/project-overview-pdr.md"
      provides: "current two-provider PDR with historical FR traceability"
      contains: "Superseded"
    - path: "docs/project-roadmap.md"
      provides: "corrected phase ledger reflecting shipped work"
      contains: "Delivered"
  key_links:
    - from: "docs/project-overview-pdr.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Superseded FR rows cross-reference REQUIREMENTS.md's Historical section (REQ-FR-07..13, REQ-FR-19)"
      pattern: "Historical"
  edge_cases:
    - category: ordering
      status: resolved
      verification: explicit
      resolution: "FR ids keep their original 1-22 numbering and table row order in project-overview-pdr.md — only the Status column changes to Superseded for FR-7..FR-13/FR-19; no row is deleted, reordered, or renumbered, preserving the 1:1 mapping to REQUIREMENTS.md's REQ-FR-07..13/19 Historical IDs. Verified via the FR-row-count grep in Task 1 (stays 22)."
  prohibitions:
    - statement: "MUST NOT delete FR-7..FR-13/FR-19 rows from project-overview-pdr.md — mark historical/superseded only, keep all 22 rows for traceability (per 05-CONTEXT.md decision: 'do not delete — traceability')"
      status: resolved
      verification: test
    - statement: "MUST NOT modify AGENTS.md or REQUIREMENTS.md — explicitly out of scope for Phase 5 per 05-CONTEXT.md Deferred Ideas"
      status: resolved
      verification: test
---

<objective>
Bring `docs/project-overview-pdr.md` and `docs/project-roadmap.md` current with the shipped two-provider (Manifest + zai-coding) architecture — the pre-2026-04-19 native 8-provider claims are corrected, superseded functional requirements are explicitly marked historical (never deleted, per traceability), and `project-roadmap.md`'s Phase 3 backlog is corrected from "Not started" to reflect what Phases 3-4 actually delivered (Docker/Compose, CI, Prometheus metrics, per-key rate limiting, transient retry).

Purpose: DOCS-01's ROADMAP success criterion requires these two docs to "describe the current two-provider (Manifest + zai-coding) architecture; superseded provider/pricing requirements are explicitly marked historical".
Output: corrected `docs/project-overview-pdr.md` (Product/Goals/FR-table/Success-Metrics/Out-of-Scope sections) and `docs/project-roadmap.md` (Phase 2/Phase 3 sections).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-routing-decision-documentation-refresh/05-CONTEXT.md
@README.md
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Correct docs/project-overview-pdr.md to the two-provider architecture</name>
  <read_first>docs/project-overview-pdr.md (full, 88 lines); .planning/REQUIREMENTS.md:101-112 (Historical section — REQ-FR-07..13, REQ-FR-19, current equivalents); AGENTS.md:1-16 (current two-provider architecture description)</read_first>
  <files>docs/project-overview-pdr.md</files>
  <action>
    Five corrections, all within `docs/project-overview-pdr.md`:

    1. **Product section (lines 7-9):** Replace with a description of exactly two providers — Manifest (`app.manifest.build`, 500+ model passthrough, default) and the z.ai GLM Coding Plan endpoint (`api.z.ai/api/coding/paas/v4`, quota-backed, key-gated `glm-*` routing). State that routing resolves via the static `MODEL_ROUTING` dict (`analytics/routing.py`) to a `(provider, model_id)` tuple. Note per-request cost calculation was removed in the 2026-04-19 Manifest cutover (`calculate_cost()` now always returns `0.0`; Manifest bills internally) and z.ai usage is tracked separately via local credit estimation (`estimate_credits()`, `GET /v1/analytics/credits`).

    2. **Goals item 2 (line 14):** Replace "Support 8+ providers through a shared OpenAI-compatible base and native SDK integration" with "Support two providers (Manifest passthrough + z.ai GLM Coding Plan) through the shared `OpenAICompatibleProvider` base — extensible to a direct third provider via the `LLMProvider` ABC only if a concrete need appears".

    3. **Functional Requirements table (lines 29-52):** Change the Status column from "Done" to "Superseded (2026-04-19)" for exactly these 8 rows: FR-7, FR-8, FR-9, FR-10, FR-11, FR-12, FR-13, FR-19 — do not delete any row. Immediately below the table, add one sentence: "FR-7 through FR-13 and FR-19 describe the pre-2026-04-19 native 8-provider architecture — see REQUIREMENTS.md's 'Historical' section for current equivalents; FR-11's *current* form (GLM routing via the z.ai coding endpoint) is Baseline, not historical." Update FR-2's Requirement text to append "(routes via `MODEL_ROUTING`; unknown non-GLM passthrough to Manifest, unknown `glm-*` passthrough to zai-coding under the effective-key gate)". Update FR-11's Requirement text to "Route `glm-*` models to the z.ai GLM Coding Plan endpoint (superseded 2026-04-19 as a native provider, re-routed 2026-09-07 to the coding endpoint — current Baseline contract, not historical)".

    4. **Success Metrics (line 67):** Replace "All 8 providers return streamed tokens end-to-end" with "Both providers (Manifest, zai-coding) return streamed tokens end-to-end".

    5. **Out of Scope (Phase 2) list (lines 75-80):** Append "(Delivered — Phase 3, see project-roadmap.md)" to the "Docker containerization" bullet, "(Delivered — Phase 4)" to "Structured logging or observability (Prometheus, OpenTelemetry)", and "(Rate limiting delivered — Phase 4; request queuing/backpressure remains N/A)" to "Request queuing or rate limiting". Leave "Conversation history persistence", "Horizontal scaling / load balancing", and "API versioning" unchanged (still out of scope, per PROJECT.md PROD-01/02/03).
  </action>
  <verify>
    <automated>grep -c "Superseded" docs/project-overview-pdr.md; grep -c "z.ai GLM Coding Plan" docs/project-overview-pdr.md; grep -c "routes requests to 8 LLM providers" docs/project-overview-pdr.md; grep -c "^| FR-" docs/project-overview-pdr.md</automated>
    <fails_when>the "Superseded" count is below 8 (not all 8 rows marked), the "z.ai GLM Coding Plan" count is 0 (two-provider architecture undocumented), the stale "8 LLM providers" phrase count is above 0, or the FR-row count is not exactly 22 (a row was deleted, breaking traceability).</fails_when>
  </verify>
  <acceptance_criteria>
    - Product section names exactly two providers with correct base URLs/mechanism.
    - FR-7..FR-13 and FR-19 (8 rows) show Status "Superseded (2026-04-19)"; all 22 original FR rows remain present.
    - "8 LLM providers"/"8+ providers" phrasing removed.
    - Out of Scope (Phase 2) list marks Docker/observability/rate-limiting as delivered.
  </acceptance_criteria>
  <done>docs/project-overview-pdr.md accurately reflects the two-provider architecture with superseded FRs marked historical, not deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Correct docs/project-roadmap.md's phase ledger to shipped reality</name>
  <read_first>docs/project-roadmap.md (full, 68 lines)</read_first>
  <files>docs/project-roadmap.md</files>
  <action>
    Three corrections, all within `docs/project-roadmap.md`:

    1. **Phase 2 deliverables (lines 22-37):** Replace the "8 providers: OpenAI, DeepSeek, MoonshotAI, Gemini, GLM, MiniMax, ByteDance" bullet with "Two providers: Manifest (500+ model passthrough) and native GLM (Z.AI) — later superseded 2026-04-19 by the Manifest cutover and 2026-09-07 by the z.ai GLM Coding Plan re-route (see project-overview-pdr.md's Historical notation)". Append to the "Model pricing table with cost calculation" bullet: "(removed 2026-04-19; `calculate_cost()` now stubs `0.0`, z.ai credit estimation tracks GLM usage instead — see `analytics/cost.py`)".

    2. **Phase 3 status + table (lines 39-56):** Change "**Status:** Not started" to "**Status:** Complete". In the feature table, append "— Delivered" to the Description cell for exactly these 7 rows: Docker image, Docker Compose, CI/CD pipeline, Prometheus metrics, Health checks, Rate limiting, Error retry. Leave the remaining 3 Low-priority rows (Secrets management, Horizontal scaling, API versioning) unchanged — still Not started, per PROJECT.md PROD-01/02/03 (deferred to v2).

    3. **Success Criteria (lines 58-62):** Change "**Phase 3**: Docker-based deployment with CI/CD and monitoring" to append "(achieved)", matching the existing Phase 1/Phase 2 "(achieved)" pattern.
  </action>
  <verify>
    <automated>grep -c "8 providers: OpenAI, DeepSeek" docs/project-roadmap.md; grep -c "Not started" docs/project-roadmap.md; grep -c "Delivered" docs/project-roadmap.md</automated>
    <fails_when>the stale "8 providers: OpenAI, DeepSeek" phrase count is above 0, the "Not started" count is above 3 (a shipped item left marked unstarted), or the "Delivered" count is below 7 (fewer than all 7 shipped Phase-3 rows marked).</fails_when>
  </verify>
  <acceptance_criteria>
    - Phase 2 deliverables describe two providers, not 8.
    - Phase 3 status is Complete; 7 of its 10 feature rows marked Delivered; the 3 Low-priority backlog rows remain Not started.
    - Phase 3 success criterion marked achieved.
  </acceptance_criteria>
  <done>docs/project-roadmap.md's phase ledger matches shipped reality — two providers, Phase 3 delivered items marked, backlog items correctly still open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Static documentation content only, no runtime surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02-01 | Information (staleness could mislead an operator or a future coding agent into re-implementing dead 8-provider code paths or a removed pricing table) | docs/project-overview-pdr.md, docs/project-roadmap.md | low | mitigate | This plan's grep-verified corrections remove the stale 8-provider/pricing-table claims that could otherwise seed a bad re-implementation |

</threat_model>

<verification>
The four Task 1 grep checks and three Task 2 grep checks all pass on the corrected files.
</verification>

<success_criteria>
- docs/project-overview-pdr.md describes exactly two providers with historical FRs marked (not deleted).
- docs/project-roadmap.md's Phase 3 reflects delivered work, leaving only genuinely-deferred backlog items open.
</success_criteria>

<output>
Create `.planning/phases/05-routing-decision-documentation-refresh/05-02-SUMMARY.md` when done.

## Artifacts Produced
- `docs/project-overview-pdr.md` — two-provider Product/Goals description; FR-7..13/FR-19 marked Superseded (traceable, not deleted)
- `docs/project-roadmap.md` — corrected Phase 2 provider count; Phase 3 marked Complete/Delivered where shipped
</output>
