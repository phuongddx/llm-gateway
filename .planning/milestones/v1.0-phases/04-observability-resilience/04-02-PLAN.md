---
phase: 04-observability-resilience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - providers/openai_compatible_base.py
  - requirements.txt
  - tests/test_openai_compatible_base.py
autonomous: true
requirements: [OBSV-03]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A transient network/5xx failure from the current provider is retried up to 2 additional times with exponential backoff+jitter before the client sees a failure"
    - "z.ai quota (429/1113) and auth (401/403) failures are never retried — the client sees the result of exactly one upstream attempt"
    - "The openai SDK's own internal retry is disabled (max_retries=0) so tenacity is the sole retry authority, closing a pre-existing silent ZAI-3 violation"
    - "After retry exhaustion, the client sees the existing provider-distinct SSE error mapping unchanged (routes/chat.py's error-mapping code is not modified)"
    - "Every retry attempt is same-provider-only — no code path can route a retry through a different provider (ZAI-3 preserved exactly)"
  artifacts:
    - path: "providers/openai_compatible_base.py"
      provides: "AsyncOpenAI(max_retries=0) construction + AsyncRetrying-wrapped create() call"
      contains: "max_retries=0"
    - path: "requirements.txt"
      provides: "tenacity>=9.0 floor-only pin"
      contains: "tenacity"
    - path: "tests/test_openai_compatible_base.py"
      provides: "OBSV-03 automated coverage incl. exclusion/exhaustion/logging edges"
  key_links:
    - from: "providers/openai_compatible_base.py"
      to: "tenacity.AsyncRetrying"
      via: "stream = await retryer(self.client.chat.completions.create, **kwargs) — constructed inline, never a @retry decorator on the async-generator method"
      pattern: "AsyncRetrying"
    - from: "providers/openai_compatible_base.py"
      to: "routes/chat.py"
      via: "reraise=True preserves the original exception's .status_code for the unchanged getattr(e, 'status_code', None) error-mapping code"
      pattern: "reraise=True"
  edge_cases:
    - requirement_id: OBSV-03
      category: unclassified
      status: resolved
      verification: test
      resolution: "The allow-list retry predicate (APIConnectionError/APITimeoutError/InternalServerError only) structurally excludes the z.ai 'status_code=402 message contains 1113' balance-exhausted exception and any 401/403 auth exception by omission, not by class name — asserted via a test that raises that exact status_code=402/'1113' exception and observes exactly one call attempt"
      probe: "unclassified — review manually"
  prohibitions:
    - requirement_id: OBSV-03
      category: privacy
      status: resolved
      verification: test
      statement: "MUST NOT log request messages or prompt content in retry-attempt logs — only the provider's base_url, attempt number, and exception class name"
      resolution: "tests/test_openai_compatible_base.py asserts the before_sleep log call's arguments never include the messages list or any of its string content"
---

<objective>
Deliver OBSV-03: same-provider-only transient retry with backoff, applied ONLY to the pre-first-token upstream call, plus the correctness fix (not called out in 04-CONTEXT.md but required by 04-RESEARCH.md) that disables the openai SDK's own silent internal retry so it cannot pre-empt ZAI-3's never-retry-quota rule.

Purpose: absorbs transient network blips/5xx from Manifest or z.ai without ever compounding retries at two layers or retrying a quota/auth failure — closing a pre-existing, previously undetected gap in ZAI-3 compliance.
Output: `providers/openai_compatible_base.py`'s constructor and `chat_stream`, `requirements.txt` + `tenacity`, `tests/test_openai_compatible_base.py`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-observability-resilience/04-CONTEXT.md
@.planning/phases/04-observability-resilience/04-RESEARCH.md
@.planning/phases/04-observability-resilience/04-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: max_retries=0 + AsyncRetrying wrap around the pre-stream create() call</name>
  <reversibility rating="reversible">tenacity is a locked, explicit NFR-02 deviation (04-CONTEXT.md Transient Retry), but the retry mechanism lives entirely behind chat_stream's existing interface — swapping it for a different implementation later touches one file, no data migration, no broken public contract.</reversibility>
  <read_first>providers/openai_compatible_base.py (full file, 67 lines), tests/test_openai_compatible_base.py (existing _provider_with_chunks/_aiter helpers), 04-RESEARCH.md Pattern 2 and Pitfalls 1-3 (full verified rationale, allow-list vs deny-list, reraise=True necessity), 04-PATTERNS.md's openai_compatible_base.py section, requirements.txt (existing floor-pin convention)</read_first>
  <files>providers/openai_compatible_base.py, requirements.txt</files>
  <behavior>
    - A provider whose mocked `create` raises `APIConnectionError` twice then returns a real chunk stream on the third call yields the expected tokens — proving the retry wrapper actually retries (not merely constructs an async generator that skips the retry, per Pitfall 1).
    - `AsyncOpenAI` is constructed with `max_retries=0` — confirmed by asserting the constructed client's `max_retries` attribute (or by observing that a mocked SDK-level 429 is never silently absorbed before tenacity sees it).
  </behavior>
  <action>
    Append `tenacity>=9.0` to `requirements.txt`, matching the existing floor-only `>=` pin convention every other line already uses (no upper bound, no pinned patch version).

    In `providers/openai_compatible_base.py`: import `AsyncRetrying`, `retry_if_exception_type`, `stop_after_attempt`, `wait_exponential_jitter` from `tenacity`, and `APIConnectionError`, `APITimeoutError`, `InternalServerError` from `openai` (alongside the existing `AsyncOpenAI` import). Define a module-level allow-list constant of exactly those three exception types — an allow-list, not a deny-list, because the z.ai "1113" balance-exhausted signal is detected by message substring at an arbitrary status code in this codebase's existing error-mapping logic (modeled in tests as `status_code=402`), so a deny-list keyed on `RateLimitError`/`AuthenticationError` by name could still retry it. In `__init__`, construct `AsyncOpenAI(api_key=api_key, base_url=self.base_url, max_retries=0)` — the SDK's own default (`max_retries=2`, internal, silent) would otherwise retry z.ai's 429 quota-exhaustion response before this provider's own retry/exclusion logic, or ZAI-3's never-retry-quota rule, ever sees the exception. In `chat_stream`, replace the direct `await self.client.chat.completions.create(**kwargs)` call with an `AsyncRetrying` instance constructed with `stop=stop_after_attempt(3)` (the locked "max 2 retries" policy == 3 total attempts), `wait=wait_exponential_jitter(initial=0.5, max=8.0)`, `retry=retry_if_exception_type(<the allow-list constant>)`, `reraise=True` (re-raises the original exception on exhaustion rather than tenacity's own `RetryError`, so the unchanged `getattr(e, "status_code", None)` logic in `routes/chat.py` keeps working), and a `before_sleep` callback that logs at `logger.warning` only `self.base_url`, the retry attempt number, and the failing exception's class name — never the `messages`/`kwargs` payload. Invoke the retryer directly on the coroutine — `stream = await retryer(self.client.chat.completions.create, **kwargs)` — never decorate the `chat_stream` async-generator method itself (decorating it retries nothing, since calling an async-generator function returns a generator object immediately without awaiting anything).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_openai_compatible_base.py -v -k retry</automated>
    <fails_when>the mocked create() is invoked only once when it was set up to fail twice then succeed (retry wrapper not actually applied, or applied to the wrong call site); or the constructed AsyncOpenAI client still uses the SDK's default max_retries.</fails_when>
  </verify>
  <acceptance_criteria>
    - requirements.txt contains tenacity>=9.0.
    - AsyncOpenAI is constructed with max_retries=0 in __init__.
    - chat_stream wraps only the create() coroutine via AsyncRetrying, never the surrounding async-for loop or the method itself.
    - A 2-fail-then-succeed transient-exception sequence yields the correct stream.
  </acceptance_criteria>
  <done>tenacity added; max_retries=0 set; AsyncRetrying wraps create() with the allow-list predicate; retry-succeeds-on-3rd-attempt test green.</done>
</task>

<task type="auto">
  <name>Task 2: Exclusion, exhaustion, and no-message-leak coverage</name>
  <read_first>tests/test_openai_compatible_base.py (as extended by Task 1), tests/test_chat_endpoint.py's ZaiQuotaError/BalanceError fixture shapes (status_code=429/402 conventions this repo already uses for the "1113" case), 04-RESEARCH.md Pitfall 3</read_first>
  <files>tests/test_openai_compatible_base.py</files>
  <action>
    Add tests to `tests/test_openai_compatible_base.py`:
    (a) a mocked `create` that raises a custom exception shaped like the existing z.ai "1113" balance-exhausted fixture (`status_code = 402`, message containing `1113`) results in exactly one call attempt and the original exception re-raised — proving the allow-list excludes it by omission rather than by exception-class name (closes the OBSV-03 unclassified edge probe);
    (b) a mocked `create` that raises an exception shaped like a 401/403 auth failure is likewise never retried (exactly one call attempt);
    (c) a mocked `create` that always raises `APIConnectionError` (all 3 attempts fail) re-raises the original `APIConnectionError` instance (not a tenacity `RetryError` wrapper) after exactly 3 call attempts — proving `reraise=True` preserves attribute access (e.g. `.status_code`) for `routes/chat.py`'s unchanged error mapping;
    (d) patch `asyncio.sleep` (tenacity's async wait calls the standard library's `asyncio.sleep` lazily inside its own portable-sleep helper) so these retry tests run in well under a second rather than waiting out real exponential backoff, keeping the plan's feedback loop fast;
    (e) a logging test asserting the `before_sleep` hook's log call arguments never include the `messages` list or any of its string content — only `base_url`, attempt number, and the exception's class name (OBSV-03 retry-logging prohibition).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_openai_compatible_base.py -v -k retry</automated>
    <fails_when>the 1113-shaped or 401/403-shaped exception is retried more than once; the exhausted-APIConnectionError case raises a wrapped RetryError instead of the original exception; the logging test finds message content in a before_sleep log call; the test file takes more than a few seconds to run (real backoff not patched out).</fails_when>
  </verify>
  <acceptance_criteria>
    - Quota/1113-shaped and auth-shaped exceptions: exactly one call attempt each, no retry.
    - Exhausted transient failure: original exception type/attributes intact after exactly 3 attempts.
    - No retry test sleeps for real backoff durations.
    - before_sleep logging never includes message content.
  </acceptance_criteria>
  <done>Full pytest tests/test_openai_compatible_base.py green in well under 4 seconds; all OBSV-03 edge/prohibition items resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gateway → z.ai/Manifest upstream | The retry boundary — every retry attempt must stay same-provider, same-client, never reroute |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02-01 | Denial of Service (self-inflicted retry amplification) | providers/openai_compatible_base.py | high | mitigate | max_retries=0 + stop_after_attempt(3) bounds total upstream calls to exactly 3 per logical request; allow-list predicate prevents amplifying retries on quota/auth failures |
| T-04-02-02 | Tampering with a locked business rule (ZAI-3: never retry quota/auth, never reroute) | providers/openai_compatible_base.py | critical | mitigate | Retry lives entirely inside chat_stream using self.client/self.base_url — no code path routes a retry through a different provider; allow-list structurally excludes quota/auth types by omission, not by name, closing the 1113-substring-match gap |
| T-04-02-03 | Information Disclosure (retry logs leaking prompt content) | providers/openai_compatible_base.py before_sleep hook | low | mitigate | before_sleep logs only base_url/attempt-number/exception-class-name — Task 2's logging test asserts no message content is ever logged |

</threat_model>

<verification>
Full suite: `.venv/bin/python -m pytest tests/ -v` must stay green (no unrelated regressions from the constructor signature change).
</verification>

<success_criteria>
- Transient errors retry up to 2 additional times with backoff, bounded to 3 total attempts.
- Quota (429/1113) and auth (401/403) errors are never retried.
- max_retries=0 closes the SDK-layer silent-retry gap.
- Retry exhaustion preserves the original exception for routes/chat.py's unchanged error mapping.
- All OBSV-03 edge-case and prohibition items resolved with passing tests.
</success_criteria>

<output>
Create `.planning/phases/04-observability-resilience/04-02-SUMMARY.md` when done.

## Artifacts Produced
- `providers/openai_compatible_base.py` — max_retries=0, AsyncRetrying-wrapped create() call, allow-list predicate, before_sleep logging
- `requirements.txt` — + tenacity>=9.0
- `tests/test_openai_compatible_base.py` — OBSV-03 coverage (happy-path retry, quota/auth exclusion, exhaustion, logging prohibition)
</output>
