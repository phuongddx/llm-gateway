---
phase: 02-analytics-retention-storage-lifecycle
plan: 3
type: execute
wave: 3
depends_on:
  - '02-02'
files_modified:
  - .env.example
  - README.md
  - AGENTS.md
autonomous: true
requirements:
  - ANLT-01
  - ANLT-02
estimate:
  tokens: 10000
  raw_tokens: 10000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'TR1 (ANLT-01): .env.example documents ANALYTICS_RETENTION_DAYS=90 with a comment line stating the 0-keeps-forever opt-out — the AGENTS.md mirror contract (env example mirrors Settings fields exactly) holds again after 02-01 introduced the field.'
    - 'TR2 (ANLT-01, ANLT-02): the README Gateway Settings table gains an ANALYTICS_RETENTION_DAYS row (Required No, Default 90) whose description states: rows older than the TTL are purged at startup and every 6 hours; 0 keeps logs forever; existing databases need the one-time sqlite3 data/analytics.db "VACUUM;" migration (gateway stopped) to make purged space reclaimable; and with the default, pre-existing rows older than 90 days — including everything already in the database — are purged on the first startup, so raise the TTL or set 0 first to keep them.'
    - 'TR3 (ANLT-02): AGENTS.md tells the truth about the shipped retention lifecycle — the analytics/db.py bullet names purge_expired and auto_vacuum=INCREMENTAL; the .env.example enumeration bullet appends ANALYTICS_RETENTION_DAYS (default 90, 0 = keep forever) plus the one-time VACUUM note; the analytics/ Key Directories row mentions the retention lifecycle; and the writer description in Code Conventions mentions the startup purge and the every-6-hours tick inside the consumer loop.'
  artifacts:
    - .env.example
    - README.md
    - AGENTS.md
  key_links:
    - '.env.example knob row ↔ config.py Settings field analytics_retention_days (AGENTS.md mirror contract — the canonical env list must match the Settings surface)'
    - 'README env-table row ↔ operator behavior shipped by 02-01/02-02 (TTL semantics, 6h cadence, 0 opt-out) ↔ the one-time VACUUM migration sanctioned for legacy data/analytics.db files (locked "docs note, not code")'
    - 'AGENTS.md analytics section ↔ the landed analytics/db.py and analytics/writer.py surfaces (purge_expired, _PURGE_INTERVAL_S tick, between-batches drain)'
  prohibitions:
    - requirement_id: ANLT-01
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: 'MUST NOT let the retention default silently destroy an operator''s pre-existing analytics history without warning — the first-startup consequence (existing rows older than the TTL are purged, which today empties the table) and the escape hatch (raise ANALYTICS_RETENTION_DAYS or set 0 before upgrading) are documented in the README env-table row, not special-cased in code (locked decision + 02-RESEARCH assumption A3; specless-probe kept prohibition, descriptor-less / flagged-unverified — doc wording is reviewed at verify-work, no automated doc tests exist in this repo).'
---

<objective>
Operator surface for the retention lifecycle (closes ANLT-01's "documented default" and ANLT-02's "documented one-liner"): .env.example gains the knob row, README gains the env-table row carrying the one-time VACUUM migration note and the first-startup consequence, and AGENTS.md's analytics/env/writer documentation catches up to the code shipped by 02-01 and 02-02.

Implements the locked CONTEXT decision "Space Reclamation" docs half ("Existing databases: documented one-time migration (plain VACUUM) — a docs note, not code") and "Retention Policy" docs half ("documented in .env.example + README env table"), plus the AGENTS.md update named in the same decision block. The decisions are LOCKED — this plan documents, it does not redesign.

Purpose: the knob is operator-usable only when it is discoverable and its destructive default consequence is warned about; AGENTS.md is the binding instruction set for every future agent session, so it must describe the purge lifecycle truthfully the moment it exists.

Output: three doc files updated to match shipped behavior; no code changes; full test suite unaffected (docs-only).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-analytics-retention-storage-lifecycle/02-CONTEXT.md
@.planning/phases/02-analytics-retention-storage-lifecycle/02-RESEARCH.md
@.planning/phases/02-analytics-retention-storage-lifecycle/02-PATTERNS.md
@.planning/phases/02-analytics-retention-storage-lifecycle/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: .env.example knob row + README env-table row with VACUUM migration note and first-startup consequence</name>
  <files>.env.example, README.md</files>
  <read_first>
  - .planning/phases/02-analytics-retention-storage-lifecycle/02-CONTEXT.md — locked "Retention Policy" (.env.example + README env table) and "Space Reclamation" (documented one-time VACUUM migration, docs note not code)
  - .planning/phases/02-analytics-retention-storage-lifecycle/02-RESEARCH.md — "Runtime State Inventory" (real data/analytics.db is legacy auto_vacuum=0; all 23 rows >90d old — first default purge empties it; the canonical question answer names the VACUUM one-liner), assumptions A2-A3 (placement latitude + README consequence sentence), "Code Examples" heading "Cutoff computation" for the TTL semantics
  - .planning/phases/02-analytics-retention-storage-lifecycle/02-PATTERNS.md — sections ".env.example" (tail-block shape to extend, .env.example:24-28) and "README.md" (Gateway Settings table at README.md:186-198, row placement beside ANALYTICS_DB_PATH at line 196, and the ANALYTICS_QUEUE_SIZE-has-no-row note)
  - .env.example (lines 24-28 — the analytics tail block: comment + ANALYTICS_DB_PATH, comment + ANALYTICS_QUEUE_SIZE)
  - README.md lines 182-199 (Configuration intro + Gateway Settings table)
  - config.py (the landed analytics_retention_days field + default — the value the docs must mirror)
  </read_first>
  <action>
  1. .env.example — append to the analytics tail block after the ANALYTICS_QUEUE_SIZE pair (line 28), same two-line comment+assignment shape and voice as the surrounding entries: comment line exactly "# Analytics log retention in days (0 = keep forever)" then "ANALYTICS_RETENTION_DAYS=90". Nothing else in the file changes.

  2. README.md — in the Gateway Settings table, insert one row directly after the ANALYTICS_DB_PATH row (line 196), before CORS_ORIGINS. Columns: Variable ANALYTICS_RETENTION_DAYS, Required No, Default 90. Description cell carries all four operator facts in this order, comma/dash separated in the table's existing telegraphic style: request-log retention in days — rows older than the TTL are purged at startup and every 6 hours; 0 keeps logs forever; the first startup with the default purges pre-existing rows older than 90 days (raise the TTL or set 0 first to keep them); existing databases reclaim purged disk space only after the one-time migration sqlite3 data/analytics.db "VACUUM;" run with the gateway stopped. Do not add an ANALYTICS_QUEUE_SIZE row (out of scope, per 02-PATTERNS note); do not touch the Legacy table or any other section.

  Keep both edits factual and comma-dense per the docs' house style; no marketing prose, no new sections. The exact VACUUM command string must match the locked one-liner from 02-RESEARCH's Runtime State Inventory byte-for-byte (sqlite3 data/analytics.db "VACUUM;").
  </action>
  <verify>
    <automated>.venv/bin/python -c "env = open('.env.example').read(); assert env.count('ANALYTICS_RETENTION_DAYS=90') == 1; assert env.count('# Analytics log retention in days (0 = keep forever)') == 1; readme = open('README.md').read(); assert readme.count('`ANALYTICS_RETENTION_DAYS`') == 1; assert 'sqlite3 data/analytics.db \"VACUUM;\"' in readme; assert 'every 6 hours' in readme; assert 'purged at startup' in readme"</automated>
    <fails_when>AssertionError — the knob assignment or its opt-out comment is missing/duplicated in .env.example, or the README row is absent, duplicated, or missing any of the four facts (TTL purge semantics, 6-hour cadence, first-startup consequence with escape hatch, one-time VACUUM migration command)</fails_when>
  </verify>
  <acceptance_criteria>
  - .env.example contains exactly one ANALYTICS_RETENTION_DAYS=90 assignment preceded by the exact opt-out comment line (counts == 1 per the verify command; the comment does not contain the token, so grep -c "ANALYTICS_RETENTION_DAYS" .env.example returns exactly 1).
  - README Gateway Settings table has exactly one row whose Variable cell is ANALYTICS_RETENTION_DAYS, placed between the ANALYTICS_DB_PATH and CORS_ORIGINS rows (python check: in the table source, the index of the ANALYTICS_DB_PATH line is less than the index of the ANALYTICS_RETENTION_DAYS line, which is less than the index of the CORS_ORIGINS line).
  - The README description cell contains: the purge cadence phrase "at startup and every 6 hours", the opt-out (the digit 0 with keep-forever wording), the first-startup consequence naming pre-existing rows with the raise-or-set-0 escape hatch, and the exact command sqlite3 data/analytics.db "VACUUM;" with the stopped-gateway qualifier.
  - No other README section changed beyond the one inserted row (the Legacy table and all other tables/sections byte-identical — confirm via git diff before committing: exactly one added line in README.md, two in .env.example).
  </acceptance_criteria>
  <done>The retention knob is discoverable at both operator entry points — .env.example and the README env table — with the destructive-default warning, the opt-out, and the one-time legacy VACUUM migration documented exactly as locked.</done>
  <reversibility rating="reversible">Two small doc edits; trivially revertible, no code impact.</reversibility>
</task>

<task type="auto">
  <name>Task 2: AGENTS.md analytics section — purge_expired, auto_vacuum, env enumeration, 6h tick</name>
  <files>AGENTS.md</files>
  <read_first>
  - .planning/phases/02-analytics-retention-storage-lifecycle/02-PATTERNS.md — section "AGENTS.md — analytics section update" (the four update spots with verbatim current text: analytics/db.py bullet at AGENTS.md:185-186, .env.example enumeration bullet at :189-194, analytics/ Key Directories row at ~:98, writer description in Code Conventions at ~:142-146)
  - .planning/phases/02-analytics-retention-storage-lifecycle/02-CONTEXT.md — locked docs decision (README env table + AGENTS.md analytics section updated)
  - .planning/phases/02-analytics-retention-storage-lifecycle/02-02-SUMMARY.md — the landed writer loop semantics these docs describe
  - AGENTS.md lines 94-105 (Key Directories table), 132-172 (Code Conventions — Async bullet), 174-194 (Important Files — analytics/db.py and .env.example bullets)
  - analytics/db.py and analytics/writer.py (landed surfaces: purge_expired signature + pragmas; _PURGE_INTERVAL_S, between-batches drain — describe what is, not what was planned)
  </read_first>
  <action>
  Four surgical prose edits in AGENTS.md's factual, comma-dense style — nothing else in the file changes:
  1. Key Directories analytics/ row (~line 98): extend the db.py purpose clause to mention the TTL retention lifecycle (purge_expired + auto_vacuum=INCREMENTAL) after the existing storage/aggregation wording.
  2. Code Conventions Async bullet (~lines 141-146): where the writer's single-consumer drain is described, append that the same consumer task runs the retention purge — once at startup as its first action and then every 6 hours (module constant), draining queued records between purge batches so streaming and write accounting are never blocked by a purge.
  3. Important Files analytics/db.py bullet (lines 185-186): after the schema/index sentence, add that AnalyticsDB provides purge_expired(retention_days) — batched rowid-subquery DELETE with per-batch commits, guarded incremental_vacuum loop and passive checkpoint — with PRAGMA auto_vacuum=INCREMENTAL set at initialize before schema creation (silently a no-op on pre-existing databases).
  4. Important Files .env.example bullet (lines 189-194): append to the env enumeration: ANALYTICS_RETENTION_DAYS (default 90, 0 = keep forever; rows older than the TTL purged at startup and every 6 hours; one-time sqlite3 data/analytics.db "VACUUM;" with the gateway stopped makes purged space reclaimable on legacy databases).
  Read the landed code before writing each edit — the docs must describe the shipped surfaces (parameter names, constants, cadence), not this plan's paraphrase of them.
  </action>
  <verify>
    <automated>.venv/bin/python -c "src = open('AGENTS.md').read(); assert src.count('purge_expired') >= 1; assert src.count('auto_vacuum=INCREMENTAL') >= 1; assert src.count('ANALYTICS_RETENTION_DAYS') >= 2; assert 'sqlite3 data/analytics.db \"VACUUM;\"' in src; assert src.count('every 6 hours') >= 1"</automated>
    <fails_when>AssertionError — any of the four update spots is missing its retention fact: no purge_expired mention (db bullet + Key Directories), no auto_vacuum pragma mention, fewer than two ANALYTICS_RETENTION_DAYS mentions (env enumeration + at least one other spot), no exact VACUUM migration command, or no 6-hour cadence phrase</fails_when>
  </verify>
  <acceptance_criteria>
  - AGENTS.md mentions purge_expired at least once, auto_vacuum=INCREMENTAL at least once, ANALYTICS_RETENTION_DAYS at least twice (env enumeration + retention prose), the exact command sqlite3 data/analytics.db "VACUUM;" once, and the every-6-hours cadence at least once (verify command).
  - The .env.example enumeration bullet now lists ANALYTICS_RETENTION_DAYS among the env vars (python check: the bullet containing "canonical list" also contains ANALYTICS_RETENTION_DAYS).
  - The Async/Code Conventions bullet describes the purge riding the existing consumer task (startup-first-action + periodic tick + between-batch drain) without introducing a second task or scheduler claim (grep -c "APScheduler\|cron" AGENTS.md returns 0).
  - git diff shows changes confined to the four named spots (Key Directories row, Async bullet, analytics/db.py bullet, .env.example bullet); the docs-drift warning block and all other sections untouched.
  </acceptance_criteria>
  <done>AGENTS.md — the binding instruction set for future sessions — describes the retention lifecycle truthfully: knob, purge method, pragma posture, 6-hour tick inside the consumer loop, and the legacy VACUUM migration, all matching the landed code.</done>
  <reversibility rating="reversible">Doc-only edits confined to four spots; trivially revertible.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs ↔ operator actions | the README row instructs a destructive-adjacent operation (one-time VACUUM, first-startup purge) — instructions must state the stopped-gateway precondition and the escape hatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Repudiation | README/AGENTS migration instructions | low | mitigate | The VACUUM one-liner is documented with the gateway-stopped precondition and the first-startup consequence carries the raise-TTL-or-set-0 escape hatch (prohibition transparency; reviewed at verify-work) |
| T-02-11 | Information Disclosure | docs describing the retention system | low | accept | Doc rows describe config semantics and aggregate behavior only — no request-content, key, or row-data examples worth protecting; repo is single-operator |

No package installs (docs-only phase) — no supply-chain row required.
</threat_model>

<verification>
- Task verifies (python -c assertions on .env.example, README.md, AGENTS.md) pass — each names the observable failure.
- Cross-check against shipped code: the documented default (90), cadence (6 hours), opt-out (0), and command string match config.py, analytics/writer.py (_PURGE_INTERVAL_S = 21600.0), and the locked VACUUM one-liner.
- Docs-only change: .venv/bin/python -m pytest tests/ -v still exits 0 (nothing executable touched; run once as the phase-final gate).
</verification>

<success_criteria>
- An operator can discover and bound retention from .env alone: the knob, its default, its opt-out, its purge cadence, its first-startup consequence with escape hatch, and the legacy space-reclamation one-liner are all documented in the two canonical operator surfaces.
- AGENTS.md matches the shipped analytics code — no future session is misled about the write/purge path.
- Phase 2 closes with every doc claim true of the running system.
</success_criteria>

## Artifacts this phase produces

Phase inventory delta (final; completes the union started in 02-01 and extended by 02-02):

- .env.example — knob row: comment "# Analytics log retention in days (0 = keep forever)" + ANALYTICS_RETENTION_DAYS=90.
- README.md — Gateway Settings table row for ANALYTICS_RETENTION_DAYS (default 90; startup + every-6-hours purge cadence; 0 opt-out; first-startup legacy consequence with escape hatch; one-time sqlite3 data/analytics.db "VACUUM;" migration, gateway stopped).
- AGENTS.md — four updated spots: analytics/ Key Directories row (retention lifecycle), Code Conventions Async bullet (purge rides the consumer task), analytics/db.py bullet (purge_expired + auto_vacuum=INCREMENTAL), .env.example enumeration (ANALYTICS_RETENTION_DAYS + VACUUM note).

<output>
Create `.planning/phases/02-analytics-retention-storage-lifecycle/02-03-SUMMARY.md` when done
</output>
