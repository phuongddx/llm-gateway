---
phase: 02-analytics-retention-storage-lifecycle
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - analytics/db.py
  - analytics/writer.py
  - main.py
  - tests/test_analytics_retention.py
  - tests/test_startup_validation.py
autonomous: true
requirements:
  - ANLT-01
  - ANLT-02
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'TR1 (ANLT-01): Settings gains analytics_retention_days: int = 90; Settings(_env_file=None) yields 90; the ANALYTICS_RETENTION_DAYS env var maps to the field (extra="ignore" keeps old .env files without the var valid); 0 is accepted and means keep-forever opt-out; any negative value fails construction with a ValidationError whose message names ANALYTICS_RETENTION_DAYS — import-time fail-fast, never a runtime purge of fresh rows.'
    - 'TR2 (ANLT-02, edge boundary explicit): purge_expired deletes exactly the request_logs rows with created_at strictly older than the cutoff — the row exactly AT the cutoff instant is retained, the row 1 microsecond older is purged, the newest row is retained (locked SQL predicate: strict less-than comparison on created_at).'
    - 'TR3 (ANLT-01, edge precision explicit): the cutoff and every stored created_at share the single canonical format produced by datetime.now(timezone.utc).isoformat(); the comparison is exact lexicographic string comparison — retention is integer days only, non-integer env values are rejected by pydantic at construction, and no rounding, truncation, or float arithmetic ever touches a timestamp.'
    - 'TR4 (ANLT-02): on a fresh file DB, initialize() executes PRAGMA auto_vacuum=INCREMENTAL strictly BEFORE executescript(_SCHEMA) so PRAGMA auto_vacuum reports 2; after a purge pass the guarded incremental_vacuum loop drives freelist_count to 0 and page_count drops — asserted via pragma values, never via file size (WAL truncation lags until checkpoint).'
    - 'TR5 (ANLT-02, edge concurrency explicit): every DELETE batch commits atomically (per-batch commit, termination on rowcount < batch); purge_expired(0) deletes nothing and returns 0; a second identical purge pass returns 0; on a legacy auto_vacuum=0 database the vacuum loop terminates via the no-decrease guard instead of spinning.'
    - 'TR6 (ANLT-02 tracer): within one real lifespan startup over a pre-seeded existing file DB, the writer task runs the startup purge as its first action — the expired row is gone, the fresh row remains, writer.last_purged reports the deleted count — and lifespan reaches request-readiness without awaiting the purge (NFR-04 startup budget untouched).'
    - 'TR7: main.py passes retention_days=settings.analytics_retention_days into the AnalyticsWriter constructor (one-line change); the writer record-path shape (try, except Exception with logger.exception, finally task_done) is byte-identical to Phase 1 and the full existing suite stays green.'
  artifacts:
    - config.py
    - analytics/db.py
    - analytics/writer.py
    - main.py
    - tests/test_analytics_retention.py
    - tests/test_startup_validation.py
  key_links:
    - 'config.py settings.analytics_retention_days → main.py lifespan AnalyticsWriter(db, queue_size=..., retention_days=settings.analytics_retention_days) → writer._purge() as the FIRST action of _run() → AnalyticsDB.purge_expired() batched rowid-subquery DELETE + guarded incremental_vacuum loop + wal_checkpoint(PASSIVE)'
    - 'analytics/db.py initialize(): PRAGMA auto_vacuum=INCREMENTAL placed strictly before executescript(_SCHEMA) — after schema creation the pragma is silently rejected by SQLite (probe P1); on the existing populated data/analytics.db it no-ops harmlessly and the README one-time VACUUM note (Plan 03) is the sanctioned fix'
    - 'purge_expired error containment: the method itself propagates DB errors (returns int for test asserts); containment lives in the writer loop — a purge failure is caught by except Exception + logger.exception and never kills the consumer task or leaks to clients'
    - 'retention knob ↔ validator: _validate_analytics_retention_days mirrors _validate_analytics_queue_size exactly (mode="after" model_validator, message names the ENV VAR because that is what the operator edits)'
  prohibitions:
    - requirement_id: ANLT-02
      category: privacy
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: 'MUST NOT emit request-log row contents (ids, model names, providers, error messages, or any per-request data) from the retention/purge logging surface — purge logs carry only aggregate counts and the TTL; the purge-correctness test asserts via caplog that the purge log line reports the deleted count and that seeded row ids and the distinctive seeded model name appear nowhere in the captured log text (specless-probe kept prohibition, descriptor-less / flagged-unverified).'
---

<objective>
Tracer slice for Phase 2: ONE end-to-end path — an operator-set retention TTL flowing from Settings through the lifespan wiring into the writer task, whose first action purges expired request_logs rows via the new batched AnalyticsDB.purge_expired — proven against a real pre-seeded database file inside a real lifespan startup.

Implements the locked CONTEXT decisions "Retention Policy" (env knob ANALYTICS_RETENTION_DAYS, int, default 90, 0 = keep-forever opt-out, applies to existing DBs with no manual SQL), "Purge Mechanism" (startup purge before/alongside writer start, batched ≈1000-row commits, never blocks streaming), and "Space Reclamation" (PRAGMA auto_vacuum=INCREMENTAL at DB creation, PRAGMA incremental_vacuum after each purge pass) — the decisions are LOCKED; this plan implements them exactly, it does not redesign them. The 6-hour periodic tick is NOT in this plan (Plan 02-02); docs are NOT in this plan (Plan 02-03).

Purpose: prove the phase's riskiest seams (cutoff string semantics on the existing schema; pragma ordering; purge-inside-consumer-task lifecycle) on the earliest commit. All mechanics are probe-verified in 02-RESEARCH.md (Patterns 1/3, Pitfalls 1/2/4/5/6) — this plan is assembly, not invention. Zero new dependencies (REQ-NFR-02).

Output: working retention knob + startup purge end-to-end, proven by a new end-to-end lifespan test, with the existing suite green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-analytics-retention-storage-lifecycle/02-CONTEXT.md
@.planning/phases/02-analytics-retention-storage-lifecycle/02-RESEARCH.md
@.planning/phases/02-analytics-retention-storage-lifecycle/02-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end retention tracer — knob → lifespan → writer startup purge → purge_expired (one path only)</name>
  <files>config.py, analytics/db.py, analytics/writer.py, main.py, tests/test_analytics_retention.py</files>
  <read_first>
  - .planning/phases/02-analytics-retention-storage-lifecycle/02-CONTEXT.md — all four locked decision blocks (Retention Policy, Purge Mechanism, Space Reclamation, Verification)
  - .planning/phases/02-analytics-retention-storage-lifecycle/02-RESEARCH.md — "Pattern 1: AnalyticsDB.purge_expired" (verbatim reference implementation), "Pattern 3: Settings knob", System Architecture Diagram, "Anti-Patterns to Avoid", Pitfalls 1, 2, 4, 5, 6
  - .planning/phases/02-analytics-retention-storage-lifecycle/02-PATTERNS.md — sections "analytics/db.py", "analytics/writer.py", "config.py + .env.example", "main.py", "Analog A — module scaffold + record factory", "Shared Patterns"
  - analytics/db.py (lines 13-102: _SCHEMA with idx_logs_created_at, initialize, write_probe, log_request with the injected-created_at line 97)
  - analytics/writer.py (whole file, 91 lines: _DRAIN_TIMEOUT_S, __init__ validation shape, enqueue put_nowait, _run loop, stop drain-then-cancel)
  - config.py (whole file: Analytics block lines 29-31, model_validator pattern lines 53-61, model_config extra="ignore")
  - main.py (lifespan lines 21-67: settings monkeypatch surface, db construction, writer construction line 58, shutdown order)
  - tests/test_analytics_db.py lines 212-236 (tmp-path file-DB raw-sqlite3 seeding pattern — the tracer test seeds the same way)
  - tests/test_startup_validation.py lines 11-12 (_settings helper), 52-61 (monkeypatch-settings + enter-lifespan test pattern)
  - tests/test_analytics_writer.py lines 19-29 (_record 12-key factory to copy)
  - AGENTS.md — Code Conventions (Async bullet, Config bullet), Testing & QA (fixtures, @pytest.mark.asyncio convention)
  </read_first>
  <behavior>
  - Test (write FIRST, must fail before implementation): test_lifespan_startup_purge_removes_expired_rows_end_to_end in tests/test_analytics_retention.py. Seed a tmp-path file DB BEFORE lifespan: raw sqlite3 connect, executescript(analytics.db._SCHEMA), insert two rows with explicit columns — id "purge-e2e-expired" with created_at (datetime.now(timezone.utc) - timedelta(days=91)).isoformat() and id "purge-e2e-fresh" with created_at datetime.now(timezone.utc).isoformat() (provider "manifest", model "gpt-4o", status "success", credits_used 0.0, remaining numeric columns 0) — commit and close. Then monkeypatch the settings singleton: app_api_key "test-key", analytics_db_path the tmp file, analytics_retention_days 90 (follow the test_startup_validation.py monkeypatch convention). Enter the real lifespan via async with lifespan(app): grab writer = app.state.analytics_writer; bounded-wait (deadline loop of asyncio.sleep(0.01), 5s cap — never a bare sleep) until writer.last_purged is not None; then read ids from the file with a fresh raw sqlite3 connection and assert the id set equals exactly {"purge-e2e-fresh"}; assert writer.last_purged == 1. Lifespan exit (writer.stop then db.close) runs in the async-with teardown.
  - The test must FAIL before implementation with AttributeError naming analytics_retention_days (monkeypatch.setattr on a missing field) or TypeError from the AnalyticsWriter constructor — either proves the path is not yet wired.
  </behavior>
  <action>
  RED: write the behavior test first (with the module scaffold from step 6), run it, confirm it fails for the wiring reason above. Then GREEN, implementing per the probe-verified patterns:

  1. config.py — in the existing # Analytics block (after analytics_queue_size, line 31) add the field analytics_retention_days with default 90 and inline comment stating 0 = keep forever (opt-out). Add a third @model_validator(mode="after") named _validate_analytics_retention_days directly after _validate_analytics_queue_size (config.py:53-61), copying that validator's structure verbatim and swapping the bound and message: reject values below 0; one-line docstring "Fail fast on negative retention — a typo must not purge fresh rows."; message text exactly: ANALYTICS_RETENTION_DAYS must be >= 0 (got {value}); 0 disables retention (keep forever) — naming the ENV VAR, showing the offending value (02-RESEARCH Pattern 3, probe R4a verified the output format). Do NOT touch model_config (extra="ignore" already keeps old .env files valid; no validate_assignment — tests monkeypatch the singleton).

  2. analytics/db.py — three additions, nothing else changed:
     (a) Module constant _PURGE_BATCH = 1000 beside _SCHEMA (the locked ≈1000-rows-per-commit batch). No new imports needed (datetime, timedelta, timezone already imported at line 5).
     (b) initialize(): insert the pragma await self._db.execute("PRAGMA auto_vacuum=INCREMENTAL") on the line BETWEEN aiosqlite.connect (line 43) and the journal_mode WAL line (line 44) — i.e. strictly before executescript(_SCHEMA); order is load-bearing (probe P1: after schema creation SQLite silently rejects the flip). After the existing migration try/except and commit, read PRAGMA auto_vacuum once; when it reports 0 (pre-existing populated file), emit one logger.info line stating auto_vacuum is off on this legacy database and that the one-time VACUUM migration documented in README enables space reclamation — informational only, never an error (Pitfall 2).
     (c) New method purge_expired with signature (self, retention_days: int, batch: int = _PURGE_BATCH, *, now: datetime | None = None) -> int — the keyword-only now parameter is the CONTEXT "Verification" locked decision "injected clock" made concrete; production callers omit it and get datetime.now(timezone.utc). Body per 02-RESEARCH Pattern 1 verbatim mechanics: guard — if the connection is missing or retention_days is 0-or-negative, return 0 (0 = keep-forever opt-out short-circuit); cutoff = (now or the real clock, minus timedelta(days=retention_days)).isoformat() — the exact same clock/format call as analytics/db.py:97; batched loop executing the DELETE whose expired-row selection is the subquery form SELECT rowid FROM request_logs WHERE created_at is less-than the bound cutoff with LIMIT bound to the batch size, outer DELETE keyed on rowid IN that subquery (this build has no direct DELETE-with-LIMIT — 02-RESEARCH probe P3a; the subquery form rides the covering index idx_logs_created_at per probe P3c); accumulate cursor.rowcount; commit after EVERY batch (short WAL checkpoints, locked); terminate when rowcount < batch. Then the guarded reclamation loop: read PRAGMA freelist_count, loop at most freelist*2+16 times executing PRAGMA incremental_vacuum and re-reading freelist_count, breaking when freelist reaches 0 or stops decreasing (exactly one page per call on SQLite 3.53.4 — probes R2/R3a; the no-decrease break also covers legacy auto_vacuum=0 files where the pragma no-ops — Pitfall 1/2); finally execute PRAGMA wal_checkpoint(PASSIVE) (non-blocking, busy=0 verified — Pitfall 5). When deleted is nonzero, logger.info exactly: Analytics retention purge: %d rows older than %dd removed — COUNT-ONLY, never row contents (see must_haves prohibitions). Return deleted. Do NOT wrap the method body in a blanket except-Exception (unlike log_request it returns int for test asserts); errors propagate to the writer loop's containment (02-PATTERNS note on analytics/db.py:69).

  3. analytics/writer.py — extend, do not redesign: __init__ gains keyword retention_days: int = 90 appended after queue_size (every current caller stays source-compatible; follow the ctor-validation precedent only if you must — retention bounds are already enforced in Settings, so no new ctor ValueError); store self._retention_days; initialize two public observables beside self.dropped: self.purges_run = 0 and self.last_purged = None (the precedent for public counters is self.dropped). Add private async _purge(): increment purges_run at entry (so bounded waits are reliable even if the purge raises), then in try/except Exception with logger.exception("Analytics retention purge failed") assign self.last_purged = await self._db.purge_expired(self._retention_days) — containment here is load-bearing: a purge failure must never kill the consumer task (all analytics writes would stop). Rewrite _run() minimally: await self._purge() as the FIRST statement (startup purge — locked "before/alongside writer start"; keeps lifespan startup instant because the purge runs inside the task, not in lifespan — NFR-04), then the existing while-loop record path stays BYTE-IDENTICAL (await get, try log_request, except Exception with logger.exception, finally task_done). Add a short comment at _purge noting mid-shutdown cancel safety: stop() may cancel the task mid-purge; each DELETE batch commits individually so at most one uncommitted batch rolls back at close and the next startup purge resumes (no code change — documentation per 02-PATTERNS writer section).

  4. main.py — one-line change at line 58: AnalyticsWriter(db, queue_size=settings.analytics_queue_size, retention_days=settings.analytics_retention_days). Nothing else in lifespan (startup purge already rides writer.start()'s task; shutdown order untouched).

  5. GREEN: run the tracer test — it must pass. Then the full suite (.venv/bin/python -m pytest tests/ -v) — the writer loop change must not disturb any Phase-1 guarantee.

  6. tests/test_analytics_retention.py — NEW file (created in the RED step): one-line module docstring naming scope (analytics retention — TTL purge lifecycle); a _record(i) dict factory copying the exact 12-key shape from tests/test_analytics_writer.py:19-29 (id f-string pattern, provider, model, token counts, latency/ttft, cost_usd, credits_used, status, error_message None); the tracer test from the behavior block carrying @pytest.mark.asyncio despite asyncio_mode = auto (repo convention). Only this ONE test in this task — correctness expansion is Task 3.

  Commit after green per AGENTS.md conventions (tracer commit: feat(02-01)).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/ -v</automated>
    <fails_when>non-zero exit code, or any test reports FAIL/ERROR in the summary — in particular tests/test_analytics_retention.py::test_lifespan_startup_purge_removes_expired_rows_end_to_end must pass (expired row purged, fresh row retained, last_purged == 1 through one real lifespan startup), and the pre-existing writer/queue/burst suites must stay green (record-path shape unchanged)</fails_when>
  </verify>
  <acceptance_criteria>
  - tests/test_analytics_retention.py exists and tests/test_analytics_retention.py::test_lifespan_startup_purge_removes_expired_rows_end_to_end passes; the test asserts the surviving id set is exactly {"purge-e2e-fresh"} and writer.last_purged == 1.
  - Knob: .venv/bin/python -c "from config import Settings; assert Settings(_env_file=None).analytics_retention_days == 90" exits 0; .venv/bin/python -c "from config import Settings; from pydantic import ValidationError
try:
    Settings(_env_file=None, analytics_retention_days=-1); raise SystemExit(1)
except ValidationError as e:
    assert 'ANALYTICS_RETENTION_DAYS' in str(e)" exits 0; the same probe with analytics_retention_days=0 constructs successfully.
  - Purge method shape: .venv/bin/python -c "from analytics.db import AnalyticsDB, _PURGE_BATCH; import inspect; sig = inspect.signature(AnalyticsDB.purge_expired); assert list(sig.parameters) == ['self', 'retention_days', 'batch', 'now']; assert sig.parameters['now'].kind is inspect.Parameter.KEYWORD_ONLY; assert _PURGE_BATCH == 1000" exits 0.
  - Pragma ordering: .venv/bin/python -c "src = open('analytics/db.py').read(); assert 0 < src.index('PRAGMA auto_vacuum=INCREMENTAL') < src.index('executescript(_SCHEMA)')" exits 0 (the pragma strictly precedes schema creation).
  - Batched subquery form: .venv/bin/python -c "src = open('analytics/db.py').read(); assert src.count('rowid IN') == 1; assert 'DELETE FROM request_logs WHERE' not in src" exits 0 (only the rowid-subquery delete exists; no direct predicate delete).
  - No blanket containment in the storage method: grep -c "except Exception" analytics/db.py returns exactly 1 (log_request only; purge_expired propagates).
  - Writer surface: .venv/bin/python -c "from analytics.writer import AnalyticsWriter; import inspect; assert list(inspect.signature(AnalyticsWriter.__init__).parameters) == ['self', 'db', 'queue_size', 'retention_days']" exits 0; a constructed instance exposes purges_run == 0 and last_purged is None before start.
  - Wiring: grep -c "retention_days=settings.analytics_retention_days" main.py returns 1.
  - Log-privacy surface: the purge log line format string in analytics/db.py contains the %d rows count and no record-dict key interpolation (grep -c "Analytics retention purge" analytics/db.py returns 1).
  - Full suite exits 0.
  </acceptance_criteria>
  <done>One real lifespan startup with retention 90 purges a pre-seeded expired row from an existing DB file and retains the fresh row — knob validated at import, purge batched+committed per batch, space reclaim wired (auto_vacuum at creation, guarded incremental_vacuum, PASSIVE checkpoint) — full suite green, tracer committed.</done>
  <reversibility rating="reversible">Pure code addition plus one constructor-argument change — single git revert restores current behavior; no schema change and no data migration (the auto_vacuum pragma only affects newly created DB files; tests run on tmp paths). Operator-facing note: against a real long-lived data/analytics.db the first startup with the default 90 DOES delete rows older than the TTL — that is the locked, documented feature semantics (README consequence lands in Plan 02-03), not a code one-way door.</reversibility>
</task>

<task type="auto">
  <name>Task 2: ANLT-01 validator coverage — default / env / 0-opt-out / negative / non-integer</name>
  <files>tests/test_startup_validation.py</files>
  <read_first>
  - .planning/phases/02-analytics-retention-storage-lifecycle/02-RESEARCH.md — "Pattern 3: Settings knob" (probe R4a: verified default, 0, env mapping, negative message, not_integer)
  - .planning/phases/02-analytics-retention-storage-lifecycle/02-PATTERNS.md — section "tests/test_config.py (and/or tests/test_startup_validation.py)" including the placement-discrepancy note and the validator-test block to mirror
  - tests/test_startup_validation.py lines 1-49 (module docstring, _settings helper, the queue-size validator pair at 35-43, the default-constructs test at 46-49)
  - config.py (the retention field + validator landed by Task 1)
  </read_first>
  <action>
  Resolving the PATTERNS placement discrepancy in favor of repo convention: the retention validator tests land in tests/test_startup_validation.py beside test_analytics_queue_size_validator_rejects_zero/negative (the established home for Settings validator tests; tests/test_config.py contains only key-resolution tests). The 02-RESEARCH Validation Architecture row pointing at tests/test_config.py is superseded by this placement — cite this resolution in the commit body.

  Append four plain sync tests after the queue-size pair (line 43), mirroring its conventions exactly (pytest.raises with ValidationError, match naming the ENV VAR; one-line docstring naming the hazard; the _settings(**kw) helper — never a second live singleton):
  - test_analytics_retention_days_default_is_90 — _settings() yields analytics_retention_days == 90 (documented default).
  - test_analytics_retention_days_zero_accepted — _settings(analytics_retention_days=0) constructs and keeps 0 (keep-forever opt-out is valid, unlike the queue-size knob whose 0 is fatal).
  - test_analytics_retention_days_negative_rejected — _settings(analytics_retention_days=-3) raises ValidationError matching ANALYTICS_RETENTION_DAYS (import-time abort naming the env var the operator edits).
  - test_analytics_retention_days_non_integer_rejected — _settings(analytics_retention_days="ninety") raises ValidationError (pydantic int parsing; no silent coercion path into the cutoff math).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_startup_validation.py -v -k retention</automated>
    <fails_when>non-zero exit code or a summary other than 4 passed — the four retention tests must exist, be selected by -k retention, and pass (negative case failing means the validator message stopped naming ANALYTICS_RETENTION_DAYS; zero case failing means the opt-out was over-rejected)</fails_when>
  </verify>
  <acceptance_criteria>
  - .venv/bin/python -m pytest tests/test_startup_validation.py -k retention -v reports exactly 4 passed, 0 failed, 0 deselected-as-error.
  - grep -c "def test_analytics_retention" tests/test_startup_validation.py returns 4.
  - The negative test carries match="ANALYTICS_RETENTION_DAYS" (grep -c "match=\"ANALYTICS_RETENTION_DAYS\"" tests/test_startup_validation.py returns at least 1).
  - The full file still passes: .venv/bin/python -m pytest tests/test_startup_validation.py -v exits 0.
  </acceptance_criteria>
  <done>ANLT-01 knob semantics (default 90, 0 = keep-forever opt-out, negative rejected naming the env var, non-integer rejected) are pinned by four conventional validator tests in the repo's established validator-test home.</done>
  <reversibility rating="reversible">Test-only addition; revertible in isolation.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Purge-correctness unit proofs — boundary trio, keep-forever, multi-batch, reclaim, legacy guard, log privacy</name>
  <files>tests/test_analytics_retention.py</files>
  <read_first>
  - .planning/phases/02-analytics-retention-storage-lifecycle/02-RESEARCH.md — "Pattern 4: Test determinism" (injected interval/timestamps/counters), "Code Examples" (cutoff computation, seeding boundary rows through the real write path), Pitfalls 1, 2, 4, 6 (single-page vacuum, legacy no-op spin, boundary direction, ISO drift)
  - .planning/phases/02-analytics-retention-storage-lifecycle/02-PATTERNS.md — "Analog D — boundary seeding + file-DB tests" (timedelta-offset seeding style, tmp_path file-DB pattern, assert pragmas never file size) and "Analog A"
  - tests/test_analytics_db.py lines 113-136 (injected old created_at through log_request), 179-188 (timedelta-offset multi-row seeding), 212-236 (raw-sqlite3 tmp-path file DB + _SCHEMA-seeded migration test)
  - tests/conftest.py (analytics_db fixture — the shared in-memory DB to reuse per AGENTS.md; do not add another db fixture)
  - analytics/db.py (purge_expired as landed by Task 1)
  </read_first>
  <behavior>
  - test_purge_expired_boundary_trio_exact_cutoff_retained: capture now = datetime.now(timezone.utc) once; seed via analytics_db.log_request three rows with injected created_at — (now - timedelta(days=90, microseconds=1)).isoformat(), (now - timedelta(days=90)).isoformat(), now.isoformat(); await analytics_db.purge_expired(90, now=now); assert return 1; assert the exactly-at-cutoff id and the fresh id survive (get_recent) and the 1µs-older id is gone.
  - test_purge_retention_zero_keep_forever_off: seed one 91-day-old row; await analytics_db.purge_expired(0); assert return 0 and the row still present (locked opt-out).
  - test_purge_multi_batch_deletes_all_expired: seed 2500 expired rows (91d old) and 300 fresh on the shared in-memory DB (seed the large set with one executemany through the fixture connection plus a single commit — cursor hygiene per 02-RESEARCH; or loop log_request); await analytics_db.purge_expired(90); assert return 2500 and get_recent total 300; a second await purge_expired(90) returns 0 (idempotent pass).
  - test_purge_reclaims_space_incremental_vacuum: tmp-path FILE DB — AnalyticsDB at that path, initialize, assert PRAGMA auto_vacuum reports 2; seed ~3000 expired + 100 fresh via executemany + one commit; capture PRAGMA page_count and freelist_count; await purge_expired(90); assert freelist_count == 0 and page_count strictly decreased. Assert via the pragma values ONLY — never the OS file-size call (WAL truncation lags until checkpoint; probe R5).
  - test_vacuum_loop_terminates_on_legacy_auto_vacuum_zero_db: raw sqlite3 creates the tmp file, executescript(analytics.db._SCHEMA), inserts 500 expired rows, closes (auto_vacuum stays 0 — the legacy posture); AnalyticsDB initialize over it (pragma silently no-ops); await asyncio.wait_for(analytics_db.purge_expired(90), timeout=10.0) completes (the no-decrease guard breaks the loop instead of spinning — Pitfall 2); assert the expired rows are gone (delete works regardless of vacuum posture).
  - test_purge_log_reports_count_and_no_row_contents: with caplog.set_level(logging.INFO, logger="analytics.db"), seed two expired rows with distinctive id strings and model "glm-5.3-purge-probe"; await purge_expired(90); assert some caplog record message contains "Analytics retention purge" and the deleted count 2; assert neither distinctive id substring nor the model probe string appears anywhere in caplog.text (prohibition TR-privacy).
  </behavior>
  <action>
  Write the behavior tests first; each must pass against Task 1's implementation — if any fails the implementation is wrong, not the test (except where the test contradicts a probe-verified quirk, in which case fix the test to the verified behavior and note it). Reuse the shared analytics_db fixture for the in-memory tests (AGENTS.md: prefer it over local duplicates); use tmp_path file DBs only where the pragma/page semantics require a real file (reclaim, legacy guard). Follow the module's existing conventions: @pytest.mark.asyncio on every async test, injected created_at through log_request for boundary rows, bounded awaits only — no wall-clock sleeps, no real 6-hour cadence anywhere (that is Plan 02-02 territory with injected intervals). For the large seeded sets prefer a single executemany of full 13-column tuples through the fixture's connection followed by one commit; keep the _record factory as the source of the column values. Get the connection for pragma reads via async context managers with fetchone (the probe-verified shape inside purge_expired itself); finalize cursors before the next pragma on file DBs (02-RESEARCH R5 cursor-hygiene note).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_analytics_retention.py -v</automated>
    <fails_when>non-zero exit code, or any test reports FAIL/ERROR — in particular: the boundary trio (exactly-at-cutoff row must be retained under the strict predicate), keep-forever opt-out (0 deletes nothing), multi-batch (2500 deleted across ≥3 committed batches, 300 retained, second pass 0), reclaim (freelist_count 0 and page_count decreased on a fresh auto_vacuum=2 file), legacy guard (bounded completion instead of an unbounded vacuum spin), and log privacy (count present, row contents absent)</fails_when>
  </verify>
  <acceptance_criteria>
  - All seven tests in the file pass (tracer + six new); .venv/bin/python -m pytest tests/test_analytics_retention.py -v exits 0.
  - grep -c "os.path.getsize" tests/test_analytics_retention.py returns 0 (pragma-based assertions only).
  - grep -c "asyncio.wait_for" tests/test_analytics_retention.py returns at least 1 (the legacy-guard test bounds the vacuum loop).
  - The boundary test injects now through the keyword-only parameter (grep -c "purge_expired(90, now=" tests/test_analytics_retention.py returns at least 1).
  - The keep-forever test asserts both the 0 return and row survival (grep -c "purge_expired(0)" tests/test_analytics_retention.py returns 1).
  - grep -c "def test_" tests/test_analytics_retention.py returns 7.
  - Full suite still exits 0.
  </acceptance_criteria>
  <done>purge_expired is proven at the storage tier: strict-cutoff boundary semantics pinned by the trio, 0-opt-out pinned, multi-batch termination + idempotence pinned, space reclamation pinned via freelist/page pragmas on a fresh INCREMENTAL file, legacy auto_vacuum=0 termination pinned (no spin), and the purge log proven count-only.</done>
  <reversibility rating="reversible">Test-only addition; revertible in isolation.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator .env → Settings → purge SQL | operator-controlled ANALYTICS_RETENTION_DAYS crosses into a destructive DELETE; the only untrusted-ish input this phase accepts |
| writer consumer ↔ single aiosqlite connection | internal; purge and writes serialize here by construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | analytics_retention_days knob (config.py → db.purge_expired) | high | mitigate | Import-time pydantic validator (>= 0, 0 = off) naming the env var — a negative TTL typo can never become a purge-everything cutoff (Task 1/2); per-purge info log with row count provides the audit trail |
| T-02-02 | Tampering | purge SQL construction (analytics/db.py) | low | mitigate | Parameterized ? binding only for cutoff and batch size — no string interpolation into SQL (02-RESEARCH Security V5); knob is pydantic-validated int before it reaches SQL |
| T-02-03 | Information Disclosure | purge logging (analytics/db.py logger) | low | mitigate | Count-only log format string; test_purge_log_reports_count_and_no_row_contents asserts seeded ids/models absent from caplog (Task 3, prohibition) |
| T-02-04 | Tampering/Repudiation | purge-vs-shutdown race (writer.stop mid-purge) | low | mitigate | Per-batch COMMIT atomicity + drain-then-cancel stop: worst case one uncommitted batch rolls back consistently and the next startup purge resumes (documented in code comment, Task 1) |
| T-02-05 | DoS | purge starving/blocking the write path | high | mitigate | Purge runs inside the consumer task (never in lifespan, never per-request); batches are sub-second at 100k-row scale (probe P3); burst-during-purge proof and inter-batch drain land in Plan 02-02 |

No package installs this phase (REQ-NFR-02; zero new dependencies per 02-RESEARCH Standard Stack) — no supply-chain row required.
</threat_model>

<verification>
- Full suite green: .venv/bin/python -m pytest tests/ -v (exit 0; any FAIL blocks the plan).
- Tracer proof: tests/test_analytics_retention.py::test_lifespan_startup_purge_removes_expired_rows_end_to_end passes — one real lifespan startup purges the expired row and retains the fresh one.
- Knob proof: .venv/bin/python -m pytest tests/test_startup_validation.py -v (4 retention tests + all Phase-1 validation tests green).
- Storage proof: .venv/bin/python -m pytest tests/test_analytics_retention.py -v (boundary trio, opt-out, multi-batch, reclaim, legacy guard, log privacy).
- Source-level gates: pragma-before-schema ordering, rowid-subquery-only delete shape, keyword-only injected clock, main.py wiring — all pinned in Task acceptance criteria as python -c assertions.
</verification>

<success_criteria>
- An operator-bound TTL flows end-to-end: Settings validates it at import, lifespan passes it to the writer, the writer task's first action purges expired rows from an existing DB file with no manual SQL (ANLT-01 core + ANLT-02 startup half).
- Purge semantics are exact: strict cutoff (at-cutoff retained, 1µs-older purged), 0 disables, batches commit individually, space is reclaimed on fresh DBs (freelist to 0), legacy DBs terminate cleanly.
- The Phase-1 write path is undisturbed: record-path shape byte-identical, full suite green, zero new dependencies.
</success_criteria>

## Artifacts this phase produces

Full phase inventory (updated by 02-02-PLAN.md and 02-03-PLAN.md; union is authoritative):

- config.py — new Settings field analytics_retention_days: int = 90 + @model_validator _validate_analytics_retention_days (>= 0; 0 = keep-forever opt-out; message names ANALYTICS_RETENTION_DAYS).
- analytics/db.py — module constant _PURGE_BATCH = 1000; initialize() sets PRAGMA auto_vacuum=INCREMENTAL before schema creation + one informational legacy notice; new method purge_expired(retention_days, batch=_PURGE_BATCH, *, now=None) -> int with batched rowid-subquery DELETE, per-batch commit, guarded incremental_vacuum loop, wal_checkpoint(PASSIVE), count-only info log. (02-02 adds the between_batches drain hook.)
- analytics/writer.py — constructor kwargs retention_days: int = 90; public observables purges_run / last_purged; private _purge() with exception containment; _run() begins with the startup purge. (02-02 adds purge_interval_s + the 6h deadline tick + _drain_queued.)
- main.py — lifespan passes retention_days=settings.analytics_retention_days to AnalyticsWriter.
- ENV VAR: ANALYTICS_RETENTION_DAYS (default 90, 0 = keep forever) — documented in .env.example + README by 02-03.
- NEW tests/test_analytics_retention.py — tracer lifespan test + boundary trio + keep-forever + multi-batch + reclaim + legacy guard + log privacy (02-02 extends with tick/block/interleave/endpoints/burst).
- tests/test_startup_validation.py — four retention validator tests.
- Docs (02-03): .env.example row, README env-table row + one-time VACUUM migration note + first-startup consequence, AGENTS.md analytics section.

<output>
Create `.planning/phases/02-analytics-retention-storage-lifecycle/02-01-SUMMARY.md` when done
</output>
