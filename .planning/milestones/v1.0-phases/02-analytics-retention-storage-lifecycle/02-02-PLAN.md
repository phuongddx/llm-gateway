---
phase: 02-analytics-retention-storage-lifecycle
plan: 2
type: execute
wave: 2
depends_on:
  - '02-01'
files_modified:
  - analytics/writer.py
  - analytics/db.py
  - tests/conftest.py
  - tests/test_analytics_retention.py
autonomous: true
requirements:
  - ANLT-02
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'TR1 (ANLT-02): the writer run loop purges again every time the deadline fires — module constant purge interval 21600.0 seconds (6 hours, constructor-injectable for tests, NOT a new env var; the locked env surface gains only ANALYTICS_RETENTION_DAYS) — with the deadline recomputed after each purge so ticks do not drift; with an injected interval of 0.05 seconds the writer reports purges_run >= 2 within a bounded sub-second wait and an expired row seeded after startup is deleted by a later pass.'
    - 'TR2 (ANLT-02, edge concurrency explicit): a deadline timeout races no records away — asyncio.wait_for cancellation on queue.get leaves a raced item queued and the get_nowait salvage picks it up (probe R3e: 3000 items, ~1850 genuine timeouts, zero lost); under a 20-stream concurrent burst with purges interleaving, every stream delivers its full token sequence terminated by [DONE], exactly 20 distinct request_logs rows appear, and writer.dropped stays 0.'
    - 'TR3 (ANLT-02): records enqueued while the consumer is inside a purge are drained between DELETE batches via the between_batches hook — none dropped, all persisted exactly once through log_request + task_done.'
    - 'TR4 (ANLT-02): lifespan request-readiness never awaits a purge — with purge_expired gated on a never-set Event, the lifespan still reaches its yield point (bounded wait on an entered-Event), proving the fire-and-forget guarantee at startup; the Phase-1 record-path shape (try, except Exception with logger.exception, finally task_done) survives the loop rewrite byte-for-byte.'
    - 'TR5 (ANLT-02): after a purge pass, /v1/analytics/summary, /v1/analytics/models, /v1/analytics/requests, and /v1/analytics/credits each report ONLY retained data — totals, model rows, request ids, and 5h/7d credit aggregates exclude purged rows while correctly including fresh manifest and zai-coding rows seeded inside the credit windows.'
    - 'TR6: the existing analytics_writer fixture default shape is unchanged for every current caller; a new analytics_retention_writer fixture provides the short-interval writer in the same lifecycle style (start, app.state injection, teardown stop).'
  artifacts:
    - analytics/writer.py
    - analytics/db.py
    - tests/conftest.py
    - tests/test_analytics_retention.py
  key_links:
    - 'analytics/writer.py _run deadline chain: loop.time() >= next_purge → _purge() → AnalyticsDB.purge_expired(retention_days, between_batches=self._drain_queued) → per-batch commit with the drain hook awaited after every batch → queue emptied of burst records mid-purge (writer.dropped stays 0)'
    - 'asyncio.wait_for(self._queue.get(), timeout=remaining) with TimeoutError → get_nowait() salvage → the record path (log_request, task_done) unchanged from Phase 1 — the tick is scheduler-only surgery around an untouched consumer body'
    - 'tests/conftest.py analytics_retention_writer(analytics_db) → AnalyticsWriter(analytics_db, queue_size=1000, retention_days=90, purge_interval_s=0.05) → app.state.analytics_writer injection (same pattern as analytics_writer; ASGITransport never runs the lifespan)'
  prohibitions: []
---

<objective>
Periodic half of the retention lifecycle (ANLT-02): the 6-hour purge tick rides the existing AnalyticsWriter consumer loop via a deadline-based wait_for wakeup (no new scheduler — locked), records enqueued mid-purge are drained between DELETE batches so fire-and-forget stays lossless, and the two integration proofs land: all four analytics endpoints serve retained data after a purge, and a 20-stream concurrent burst completes unblocked with purges interleaving.

Builds directly on 02-01's artifacts (purge_expired, writer startup purge, retention knob). Implements the locked CONTEXT decisions "Purge Mechanism" (every 6 hours inside the existing AnalyticsWriter background loop — no new scheduler dependency; never blocks response streaming; WAL concurrent reads keep working) and "Verification" (periodic scheduling tested via injected short intervals; streaming-not-blocked-during-purge tested with the Phase-1 burst recipe) — the decisions are LOCKED; this plan implements them exactly.

Purpose: the tick is the last mechanical risk on the purge path (a scheduler bug either never purges or eats records); proving it deterministically plus the two integration proofs closes ANLT-02. All mechanics are probe-verified in 02-RESEARCH.md (Pattern 2 + addendum, probes R3d/R3e/P5) — assembly, not invention. Zero new dependencies.

Output: deadline-scheduled 6h purge tick + inter-batch drain + conftest fixture + five new proofs, full suite green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-analytics-retention-storage-lifecycle/02-CONTEXT.md
@.planning/phases/02-analytics-retention-storage-lifecycle/02-RESEARCH.md
@.planning/phases/02-analytics-retention-storage-lifecycle/02-PATTERNS.md
@.planning/phases/02-analytics-retention-storage-lifecycle/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 6h deadline tick inside the writer loop + inter-batch queue drain + no-block proof</name>
  <files>analytics/writer.py, analytics/db.py, tests/conftest.py, tests/test_analytics_retention.py</files>
  <read_first>
  - .planning/phases/02-analytics-retention-storage-lifecycle/02-CONTEXT.md — locked "Purge Mechanism" (6h inside the existing loop, no new scheduler) and "Verification" (injected intervals, deterministic)
  - .planning/phases/02-analytics-retention-storage-lifecycle/02-RESEARCH.md — "Pattern 2: Deadline-scheduled purge tick inside the writer loop" (verbatim prototype, probe R3d end-to-end), "Inter-batch queue drain" addendum, probe R3e (wait_for losslessness + salvage), Pitfall 3 (purge-starved queue drops), "Open Questions" 1-2 (observability attrs + PASSIVE checkpoint dispositions)
  - .planning/phases/02-analytics-retention-storage-lifecycle/02-PATTERNS.md — "analytics/writer.py" section (the loop being modified, ctor signature-extension shape, shutdown/drain pattern), "tests/conftest.py" section (fixture constraint: keep the default shape for current callers), "Shared Patterns" (fire-and-forget error containment, deterministic async testing)
  - .planning/phases/02-analytics-retention-storage-lifecycle/02-01-SUMMARY.md — exact purge_expired / _purge / ctor surface as landed
  - analytics/writer.py (as landed by 02-01: ctor, _purge, _run with startup purge, stop)
  - analytics/db.py (purge_expired batch loop as landed by 02-01)
  - tests/conftest.py (analytics_writer fixture lines 24-33 — the style to copy)
  - tests/test_analytics_writer.py lines 81-107 (Event-gate + sleep(0) scheduling-test conventions)
  - tests/test_analytics_retention.py (module as landed by 02-01 — _record factory, existing tests)
  </read_first>
  <behavior>
  - test_periodic_tick_purges_reseeded_expired_rows: with the analytics_retention_writer fixture (interval 0.05s), after startup, seed one row with created_at 91 days old via log_request; bounded await asyncio.sleep(0.25); assert writer.purges_run >= 2 and the seeded id is gone from get_recent (a later pass, not the startup pass, removed it).
  - test_startup_purge_does_not_block_lifespan_readiness: monkeypatch settings to a tmp-path DB with retention 90 and app_api_key set; monkeypatch AnalyticsDB.purge_expired (class level) with an async wrapper that increments a counter then awaits a never-set-until-later Event before delegating; run the real lifespan in a task; assert an entered-Event set inside the async-with completes within a bounded 2s wait WHILE the gate is still closed (purge has not returned) and the wrapper counter is >= 1; then set the gate, await the lifespan task to completion within a bounded 5s.
  - test_interleave_queue_drained_between_purge_batches: seed 2500 expired rows on the fixture DB (executemany + single commit, 02-01 Task 3 style); construct AnalyticsWriter(analytics_db, queue_size=1000, retention_days=90, purge_interval_s=5.0) inline; start it; immediately (synchronously, no await between) enqueue 30 fresh records via writer.enqueue; bounded await asyncio.sleep(0.3) so the startup purge completes its batches; assert writer.dropped == 0, get_recent total == 30 (expired gone, fresh persisted), and writer.wait_drained(5.0) returns with the queue empty; stop the writer in teardown.
  - test_purge_between_batches_hook_awaited_per_batch: on the shared in-memory DB seed 2500 expired rows; define an async callback counting invocations; await analytics_db.purge_expired(90, between_batches=counter); assert the counter equals the number of batches (2500 rows at batch 1000 → 3: the hook fires after every batch commit including the final partial one) and the return is 2500.
  </behavior>
  <action>
  Write the four behavior tests first (they must fail: the tick does not exist, the hook does not exist, the fixture does not exist). Then implement:

  1. analytics/db.py — extend purge_expired with one keyword-only parameter between_batches (a callable returning an awaitable, default None) appended after now; import the callable/awaitable types from collections.abc. Inside the batch loop, after each per-batch commit (including the final partial batch), await the hook when it is not None. Nothing else in the method changes — cutoff, subquery delete, rowcount termination, vacuum loop, checkpoint, count-only log all stay exactly as 02-01 landed them.

  2. analytics/writer.py —
     (a) Module constant _PURGE_INTERVAL_S = 6 * 3600.0 beside _DRAIN_TIMEOUT_S (module constant, NOT env — locked single-knob env surface; constructor-injectable is the test seam, per 02-RESEARCH assumption A1).
     (b) __init__ gains keyword purge_interval_s: float = _PURGE_INTERVAL_S appended after retention_days; store it privately.
     (c) New private coroutine _drain_queued(): loop get_nowait off the queue; for each record call log_request inside try/except Exception with logger.exception, task_done in finally (mirroring the _run record path so join() semantics and containment are identical); break on QueueEmpty.
     (d) _purge() now calls purge_expired with between_batches=self._drain_queued (the Pitfall-3 mitigation: during a long purge, burst records are persisted between DELETE batches instead of filling the 1000-cap and dropping).
     (e) Rewrite _run() per 02-RESEARCH Pattern 2 verbatim mechanics, keeping the Phase-1 record path byte-identical: startup purge stays the first action; then next_purge = loop.time() + self._purge_interval_s; each iteration computes timeout = max(0.0, next_purge - loop.time()) and awaits asyncio.wait_for(self._queue.get(), timeout=timeout); on asyncio.TimeoutError set record = None and attempt the get_nowait salvage guarded by except QueueEmpty (a raced item stays queued — cancellation-safe on this interpreter, probe R3e; the salvage only reduces pickup latency); when a record was obtained, keep the exact try / log_request / except Exception with logger.exception / finally task_done block; after the record handling, when loop.time() >= next_purge await self._purge() and recompute next_purge = loop.time() + self._purge_interval_s (no drift).

  3. tests/conftest.py — add ONE new fixture after analytics_writer, same lifecycle style (lazy from main import app inside, app.state injection, teardown stop): analytics_retention_writer(analytics_db) constructing AnalyticsWriter(analytics_db, queue_size=1000, retention_days=90, purge_interval_s=0.05). Do NOT alter the existing analytics_writer fixture or the client fixture — every current caller keeps the default shape (02-PATTERNS constraint; 02-RESEARCH Wave-0 gap).

  4. GREEN: the four behavior tests pass; then the Phase-1 regression set — the writer rewrite must leave every queue/burst/disconnect guarantee intact.

  Commit per AGENTS.md conventions (feat(02-02)).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_analytics_retention.py -v -k "tick or block or interleave or hook" && .venv/bin/python -m pytest tests/test_analytics_writer.py tests/test_analytics_endpoints.py tests/test_chat_endpoint.py -v</automated>
    <fails_when>either command exits non-zero or reports FAIL/ERROR — in particular: the tick test (purges_run >= 2 with reseeded row purged by a later pass), the no-block test (lifespan enters while the purge is still gated), the interleave test (dropped == 0 with 30 mid-purge records persisted), the hook test (3 invocations for 2500 rows), and every Phase-1 writer/endpoint/chat test (the loop rewrite must not disturb drain, burst, disconnect, or error-frame guarantees)</fails_when>
  </verify>
  <acceptance_criteria>
  - .venv/bin/python -m pytest tests/test_analytics_retention.py -k "tick or block or interleave or hook" -v reports 4 passed.
  - Writer surface: .venv/bin/python -c "from analytics.writer import AnalyticsWriter, _PURGE_INTERVAL_S; import inspect; params = list(inspect.signature(AnalyticsWriter.__init__).parameters); assert params == ['self', 'db', 'queue_size', 'retention_days', 'purge_interval_s']; assert _PURGE_INTERVAL_S == 21600.0" exits 0.
  - Deadline wakeup present exactly once: grep -c "asyncio.wait_for(self._queue.get()" analytics/writer.py returns 1; salvage present: grep -c "get_nowait" analytics/writer.py returns at least 2 (salvage in _run + drain loop).
  - Drain hook wiring: grep -c "between_batches=self._drain_queued" analytics/writer.py returns 1; grep -c "between_batches" analytics/db.py returns at least 2 (parameter + await site).
  - Record-path preservation: grep -c "task_done" analytics/writer.py returns at least 2 (record path + drain); the log_request call sites in _run and _drain_queged both sit inside try/except Exception with logger.exception (source assertion via python ast: every call to log_request in analytics/writer.py is inside a Try node whose handlers catch Exception).
  - Fixture: grep -c "analytics_retention_writer" tests/conftest.py returns at least 1; the existing analytics_writer fixture body is unchanged (grep -c "AnalyticsWriter(analytics_db, queue_size=1000)" tests/conftest.py returns 1 — the default-shape construction survives verbatim).
  - No new env surface: grep -c "ANALYTICS_PURGE" analytics/writer.py config.py returns 0 across both (the interval stays a module constant, not a knob).
  - Full suite exits 0.
  </acceptance_criteria>
  <done>The 6-hour purge tick rides the existing consumer loop (deadline wait_for, no drift, salvage lossless), mid-purge burst records drain between batches with zero drops, lifespan readiness provably never awaits a purge, and every Phase-1 writer guarantee still holds — full suite green.</done>
  <reversibility rating="reversible">Loop rewrite + one keyword parameter + one fixture — single revert restores the 02-01 state; no data, schema, or interface changes beyond the additive keyword.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration proofs — endpoints serve retained data post-purge; 20-stream burst unblocked during purge</name>
  <files>tests/test_analytics_retention.py</files>
  <read_first>
  - .planning/phases/02-analytics-retention-storage-lifecycle/02-CONTEXT.md — locked "Verification" (endpoints serve retained data post-purge; streaming not blocked during purge)
  - .planning/phases/02-analytics-retention-storage-lifecycle/02-RESEARCH.md — "Validation Architecture" rows for endpoints-post-purge and burst-during-purge; Pitfall 3 warning signs (writer.dropped > 0)
  - .planning/phases/02-analytics-retention-storage-lifecycle/02-PATTERNS.md — "Analog C — burst / not-blocked-during-purge" (tests/test_analytics_writer.py:217-266 conventions: slowed_log attribute assignment, patch of routes.chat module-level functions, gather of client.post, exactly-once id-set asserts) and "Analog E — endpoints-post-purge" (tests/test_analytics_endpoints.py:41-61 seeding + 142-157 credit-window seeding)
  - tests/test_analytics_writer.py lines 217-276 (the 20-stream burst recipe to replay — slowed writer, provider patching, token-delivery asserts)
  - tests/test_analytics_endpoints.py lines 41-61 (endpoint test shape) and 142-157 (credits aggregation seeding with timedelta offsets)
  - tests/conftest.py (client + auth_headers + analytics_db fixtures)
  - tests/test_analytics_retention.py (module as it stands after Task 1)
  </read_first>
  <behavior>
  - test_endpoints_serve_retained_data_after_purge: on the fixture DB seed four rows via log_request with injected created_at — a 91-day-old manifest row and a 91-day-old zai-coding row with credits_used 120 (both purge fodder, the zai row OUTSIDE the 7d window anyway), plus a fresh manifest row and a fresh zai-coding row with credits_used 50 seeded at now minus one hour (inside the 5h window); await analytics_db.purge_expired(90); then through client + auth_headers: GET /v1/analytics/summary asserts total_requests == 2 and token totals exclude the old rows; GET /v1/analytics/models asserts the old rows' model is absent and only fresh model rows appear; GET /v1/analytics/requests asserts the old ids are absent and both fresh ids present; GET /v1/analytics/credits asserts the 5h and 7d used aggregates equal the fresh zai row's contribution only (50) and the per-model breakdown contains no purged model.
  - test_burst_streams_unblocked_during_purge: replay the 20-stream burst recipe from tests/test_analytics_writer.py:217-276 — slowed log_request via attribute assignment on the fixture DB (await asyncio.sleep(0.005) before delegating; deterministic lag, never a blocking sleep), patch routes.chat.create_provider with side_effect fakes and routes.chat.resolve_provider to ("manifest", "gpt-4o"), each fake provider yielding a small distinct token sequence; BEFORE the burst, seed ~1500 expired rows (executemany + one commit) and swap app.state.analytics_writer to an inline-constructed AnalyticsWriter(analytics_db, queue_size=1000, retention_days=90, purge_interval_s=0.03) that you start (routes read app.state per request, so the swap takes effect for every burst request); fire asyncio.gather of 20 client.post /v1/chat/completions; assert all 20 return 200, every body contains its full expected token sequence and ends with the DONE sentinel frame; after writer.wait_drained(5.0), assert exactly 20 distinct new request_logs rows for the burst ids (get_recent paging if needed), writer.dropped == 0, and the seeded expired rows are gone (purges interleaved with the burst). Stop the inline writer in a finally block.
  </behavior>
  <action>
  Write both tests first; they must pass against Task 1's implementation. Follow the Phase-1 deterministic conventions exactly: monkeypatch by attribute assignment on the fixture's db instance for the slowed writer, unittest.mock.patch ONLY for module-level functions (create_provider, resolve_provider), no monkeypatch fixture, no app.dependency_overrides; @pytest.mark.asyncio on both tests; bounded sub-second awaits only (the 0.03s purge interval and 0.005s per-write lag are the determinism levers — never wall-clock sleeps sized to the 6h cadence). For the credits assertions reuse the response-field expectations from tests/test_analytics_endpoints.py:142-157 (read the test to get the exact JSON field names before writing asserts — do not guess field names). For exactly-once, assert on the SET of burst ids observed in request_logs (20 unique, no duplicates, no stragglers from prior tests since the fixture DB is function-scoped). The inline ticking writer must be stopped in teardown even on assertion failure (try/finally around the burst), and the fixture writer's app.state slot is not restored — the client fixture teardown does not depend on it.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_analytics_retention.py -v -k "endpoints or burst" && .venv/bin/python -m pytest tests/ -v</automated>
    <fails_when>either command exits non-zero or reports FAIL/ERROR — in particular: the endpoints test (all four endpoints must exclude the two purged rows and correctly aggregate the fresh zai credits), the burst test (all 20 streams deliver full token sequences + DONE, exactly-once rows, dropped == 0 with expired rows purged mid-burst), and the full-suite run (baseline endpoints and Phase-1 guarantees unregressed)</fails_when>
  </verify>
  <acceptance_criteria>
  - .venv/bin/python -m pytest tests/test_analytics_retention.py -k "endpoints or burst" -v reports 2 passed.
  - The endpoints test exercises all four analytics routes (grep on the test function body via python: the single test source references summary, models, requests, and credits route paths).
  - The burst test constructs a ticking writer (grep -c "purge_interval_s=0.03" tests/test_analytics_retention.py returns 1) and asserts writer.dropped == 0 (grep -c "dropped == 0" tests/test_analytics_retention.py returns at least 1).
  - No real-cadence waits: the file contains no sleep with an argument >= 1 (python check: no call to asyncio.sleep with a numeric literal >= 1).
  - .venv/bin/python -m pytest tests/test_analytics_retention.py -v reports 13 passed total (tracer + 6 storage proofs + 4 scheduling proofs + 2 integration proofs).
  - Full suite exits 0.
  </acceptance_criteria>
  <done>ANLT-02's integration contract is proven: post-purge the four analytics endpoints serve exactly the retained data (credits included), and a concurrent 20-stream coding burst completes with full delivery, exactly-once rows, and zero drops while purges run — full suite green.</done>
  <reversibility rating="reversible">Test-only addition; revertible in isolation.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE request path ↔ writer consumer task | streaming generators enqueue while the consumer may be inside purge_expired — the phase's core non-blocking guarantee lives at this seam |
| analytics endpoints ↔ retained DB state | readers must never observe a torn purge (WAL snapshots) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | DoS | purge monopolizing the single consumer (starved writes, queue overflow, record loss) | high | mitigate | Inter-batch drain hook (between_batches=self._drain_queued) keeps the queue empty mid-purge; test_interleave + burst tests pin dropped == 0 (Pitfall 3); batches are sub-second at 100k scale (probe P3) |
| T-02-07 | DoS | tick scheduler stalling the record path (missed records at deadline wakeups) | medium | mitigate | wait_for + get_nowait salvage on the cancellation-safe asyncio.Queue.get (probe R3e: zero loss at 3000 items); exactly-once burst proof covers the raced-item path |
| T-02-08 | Information Disclosure | endpoint responses after purge (stale/torn reads) | low | mitigate | WAL snapshot isolation during the batched purge (probe P5: 412 concurrent reads, zero failures); endpoints test pins retained-only data; wal_checkpoint(PASSIVE) keeps the WAL bounded without blocking |
| T-02-09 | DoS | lifespan startup blocked by a huge backlog purge (NFR-04 breach) | medium | mitigate | Startup purge remains the consumer task's first action, never an awaited lifespan step; test_startup_purge_does_not_block_lifespan_readiness pins readiness with a gated purge |

No package installs this phase (REQ-NFR-02) — no supply-chain row required.
</threat_model>

<verification>
- Full suite green: .venv/bin/python -m pytest tests/ -v (exit 0; any FAIL blocks the plan).
- Scheduling proof: tests/test_analytics_retention.py -k "tick or block or interleave or hook" — 4 passed.
- Integration proof: tests/test_analytics_retention.py -k "endpoints or burst" — 2 passed.
- Phase-1 regression proof: tests/test_analytics_writer.py + tests/test_analytics_endpoints.py + tests/test_chat_endpoint.py green after the _run rewrite.
- Source gates: _PURGE_INTERVAL_S == 21600.0 module constant; between_batches wiring on both sides; default fixture shape unchanged; no new env knob.
</verification>

<success_criteria>
- Rows older than the TTL are purged automatically at startup AND periodically (default 6h, deterministic short-interval proof), with no drift and zero record loss at deadline wakeups.
- All four analytics endpoints serve retained data correctly after a purge, credits included.
- Purging never blocks or delays response streaming: 20 concurrent streams deliver fully, exactly-once, zero drops, with purges interleaving; lifespan readiness never awaits a purge.
- On-disk growth stays bounded and reclaimed space is real (02-01's reclaim proof + the drain keeping the write path lossless); WAL reads keep working during purge.
</success_criteria>

## Artifacts this phase produces

Phase inventory delta (extends 02-01's list; 02-03 appends the docs):

- analytics/writer.py — module constant _PURGE_INTERVAL_S = 21600.0; constructor kwargs purge_interval_s: float = _PURGE_INTERVAL_S; deadline-scheduled _run (wait_for wakeup + get_nowait salvage + no-drift next_purge recompute); private _drain_queued(); _purge passes between_batches.
- analytics/db.py — purge_expired keyword-only between_batches hook awaited after every per-batch commit.
- tests/conftest.py — new analytics_retention_writer fixture (retention 90, interval 0.05s); existing fixtures untouched.
- tests/test_analytics_retention.py — five new proofs: periodic tick, startup no-block, inter-batch interleave, between-batches hook, endpoints-post-purge, burst-during-purge (file totals 13 tests).

<output>
Create `.planning/phases/02-analytics-retention-storage-lifecycle/02-02-SUMMARY.md` when done
</output>
