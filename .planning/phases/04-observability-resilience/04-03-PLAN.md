---
phase: 04-observability-resilience
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - config.py
  - rate_limiter.py
  - routes/chat.py
  - .env.example
  - tests/test_rate_limiting.py
  - tests/test_startup_validation.py
autonomous: true
requirements: [OBSV-02]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A client exceeding RATE_LIMIT_PER_KEY (same Bearer token) receives HTTP 429 before any provider is constructed or called"
    - "The existing per-IP RATE_LIMIT continues to apply independently alongside the new per-key limit — both apply"
    - "RATE_LIMIT_PER_KEY is configurable via .env and fails fast (ValidationError naming the variable) at Settings construction if malformed"
    - "A request with a missing/malformed Bearer token still gets a per-key-scoped (IP-scoped) limit rather than being exempt from per-key limiting entirely"
  artifacts:
    - path: "config.py"
      provides: "rate_limit_per_key field + _validate_rate_limit_per_key validator"
      contains: "rate_limit_per_key"
    - path: "rate_limiter.py"
      provides: "extract_bearer_key(request) -> str key_func"
      exports: ["extract_bearer_key"]
    - path: "routes/chat.py"
      provides: "second stacked @limiter.limit() decorator on the chat route, keyed by extract_bearer_key"
      contains: "extract_bearer_key"
    - path: "tests/test_rate_limiting.py"
      provides: "OBSV-02 automated coverage incl. boundary/prohibition edges"
  key_links:
    - from: "routes/chat.py"
      to: "rate_limiter.py"
      via: "@limiter.limit(settings.rate_limit_per_key, key_func=extract_bearer_key) stacked beneath the existing per-IP @limiter.limit(settings.rate_limit)"
      pattern: "extract_bearer_key"
    - from: "config.py"
      to: ".env.example"
      via: "RATE_LIMIT_PER_KEY row documents the new Settings field's default"
      pattern: "RATE_LIMIT_PER_KEY"
  edge_cases:
    - requirement_id: OBSV-02
      category: boundary
      status: resolved
      verification: test
      resolution: "Exactly N requests (the configured limit, monkeypatched low for test speed) with the same Bearer token all succeed with no 429; the N+1th request 429s — both sides of the threshold asserted in tests/test_rate_limiting.py"
      probe: "What happens exactly at each min/max/threshold — and one step either side?"
    - requirement_id: OBSV-02
      category: precision
      status: dismissed
      reason: "Rate-limit window arithmetic (fixed-window or moving-window bucketing) is owned entirely by slowapi/limits' own in-memory storage backend, not bespoke code in this repo; no custom precision surface exists here beyond the upstream library's own test suite."
      probe: "Where can precision loss, overflow, or rounding/tie-breaking occur — and what is the exact contract?"
  prohibitions:
    - requirement_id: OBSV-02
      category: privacy
      status: resolved
      verification: test
      statement: "MUST NOT log the raw Bearer token value anywhere in rate_limiter.py or the chat route's per-key limiter wiring"
      resolution: "tests/test_rate_limiting.py includes a source-scan assertion that rate_limiter.py contains no logger/logging call referencing the extracted token variable"
---

<objective>
Deliver OBSV-02: per-key rate limiting stacked alongside the existing per-IP limit on `/v1/chat/completions`, keyed by the Bearer token with an IP fallback, configurable via a new `RATE_LIMIT_PER_KEY` env var that fails fast at Settings construction if malformed.

Purpose: bounds cost/quota burn per credential (not just per network address) before any provider call is made, closing a gap the existing per-IP-only limiter left open.
Output: `config.py`'s new field+validator, `rate_limiter.py`'s new key_func, `routes/chat.py`'s stacked decorator, `.env.example`'s new row, `tests/test_rate_limiting.py`.

Depends on 04-01 because both plans touch `routes/chat.py` (04-01 adds the metrics-recording call; this plan adds the stacked rate-limit decorator on the same route) — sequenced to avoid a same-wave file conflict, not because of a functional dependency.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-observability-resilience/04-CONTEXT.md
@.planning/phases/04-observability-resilience/04-RESEARCH.md
@.planning/phases/04-observability-resilience/04-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Wire RATE_LIMIT_PER_KEY end-to-end (config → key_func → stacked decorator)</name>
  <precondition>routes/chat.py already imports `limiter` from rate_limiter and carries the existing `@limiter.limit(settings.rate_limit)` decorator on the chat route (Phase 1 baseline, unchanged by 04-01's metrics-recording addition).</precondition>
  <read_first>config.py (full file), rate_limiter.py (full file), routes/chat.py:1-64 (imports + chat route + verify_auth), 04-PATTERNS.md's rate_limiter.py/routes-chat.py/config.py sections, 04-RESEARCH.md Pattern 3 and Assumption A2/A3</read_first>
  <files>config.py, rate_limiter.py, routes/chat.py, .env.example</files>
  <behavior>
    - With `settings.rate_limit_per_key` monkeypatched to `"2/minute"`, two requests carrying the same Bearer token succeed; a third request with that same token within the window returns 429, and the patched `create_provider` was called exactly twice (never on the 429 path).
    - `Settings(rate_limit_per_key="bogus/zzz")` raises `ValidationError` naming `RATE_LIMIT_PER_KEY`.
  </behavior>
  <action>
    In `config.py`: add `rate_limit_per_key: str = "60/minute"` directly below the existing `rate_limit` field (matches the existing per-IP default absent a locked stricter/looser number in 04-CONTEXT.md), plus a `_validate_rate_limit_per_key` `@model_validator(mode="after")` identical in shape to the existing `_validate_rate_limit` — same `parse_many` try/except/raise structure, `RATE_LIMIT_PER_KEY` substituted throughout the error messages.

    In `rate_limiter.py`: import `Request` from `starlette.requests`; add `extract_bearer_key(request: Request) -> str` reading `request.headers.get("authorization", "")`, stripping via the same `.removeprefix("Bearer ").removeprefix("bearer ")` chain `routes/chat.py`'s `verify_auth` already uses, and falling back to `get_remote_address(request)` when the stripped token is empty (locked decision: missing/malformed tokens still get an IP-scoped limit, never an exemption).

    In `routes/chat.py`: import `extract_bearer_key` alongside the existing `limiter` import; stack `@limiter.limit(settings.rate_limit_per_key, key_func=extract_bearer_key)` directly beneath the existing `@limiter.limit(settings.rate_limit)` on the `chat` route — decoration order does not affect slowapi's accumulation mechanics, but keep the existing decorator first for diff-minimality.

    Add a `RATE_LIMIT_PER_KEY=60/minute` row to `.env.example` directly below the existing `RATE_LIMIT` row, with a comment noting both limits apply.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_rate_limiting.py -v</automated>
    <fails_when>a third same-token request within the low monkeypatched window does not 429, or create_provider is invoked a third time before the 429 fires; Settings() construction with a valid default rate_limit_per_key raises unexpectedly.</fails_when>
  </verify>
  <acceptance_criteria>
    - config.py has rate_limit_per_key field + validator mirroring _validate_rate_limit exactly.
    - rate_limiter.py exports extract_bearer_key with the documented fallback.
    - routes/chat.py's chat route carries both stacked @limiter.limit() decorators.
    - .env.example documents RATE_LIMIT_PER_KEY.
  </acceptance_criteria>
  <done>Per-key 429-before-provider-call test green; .env.example and config.py updated; both rate limits apply independently.</done>
</task>

<task type="auto">
  <name>Task 2: Boundary, dual-limit, validator, and no-log-leak coverage</name>
  <read_first>tests/test_rate_limiting.py from Task 1, tests/test_startup_validation.py's existing test_rate_limit_validator_* pattern, tests/test_chat_endpoint.py's patch("routes.chat.create_provider", ...) convention</read_first>
  <files>tests/test_rate_limiting.py, tests/test_startup_validation.py</files>
  <action>
    Add tests to `tests/test_rate_limiting.py`:
    (a) boundary — with `rate_limit_per_key` monkeypatched to a low N, exactly N requests (same Bearer token) all succeed with no 429, and only the N+1th 429s (closes the OBSV-02 boundary edge probe, one step either side of the threshold);
    (b) dual-limit — a request whose per-key limit is exceeded but whose per-IP `RATE_LIMIT` is not yet exceeded still returns 429 (both limits are independently enforced, per 04-CONTEXT.md "both apply");
    (c) a source-scan prohibition check — read `rate_limiter.py`'s source text and assert no `logger`/`logging` call references the extracted-token local variable name anywhere in the file (OBSV-02 privacy prohibition: the raw Bearer-token rate-limit key is never written to logs).

    Add to `tests/test_startup_validation.py` (mirroring the existing `test_rate_limit_validator_rejects_garbage`/`test_rate_limit_validator_rejects_wrong_granularity`/`test_rate_limit_validator_rejects_bare_number` trio exactly, new field): `test_rate_limit_per_key_validator_rejects_garbage`, asserting `Settings(rate_limit_per_key="bogus/zzz")` raises `ValidationError` matching `RATE_LIMIT_PER_KEY`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_rate_limiting.py tests/test_startup_validation.py -v</automated>
    <fails_when>the boundary test's Nth request 429s when it should succeed, or the N+1th succeeds when it should 429; the dual-limit test's per-key-exceeded-but-per-IP-ok case does not 429; the source-scan finds a logger call referencing the token; the new validator test does not raise ValidationError naming RATE_LIMIT_PER_KEY.</fails_when>
  </verify>
  <acceptance_criteria>
    - Boundary test proves both sides of the exact-limit threshold.
    - Dual-limit independence proven.
    - No logging of the raw token anywhere in rate_limiter.py.
    - RATE_LIMIT_PER_KEY validator rejects malformed input at construction time.
  </acceptance_criteria>
  <done>Full pytest tests/test_rate_limiting.py tests/test_startup_validation.py green; all OBSV-02 edge/prohibition items resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → per-key/per-IP rate limiter (before route body) | Both limits evaluated before any provider call or cost is incurred |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03-01 | Elevation of Privilege / DoS (rate-limit bypass via missing/garbage Bearer token) | rate_limiter.py extract_bearer_key | medium | mitigate | Falls back to get_remote_address so a request with no/garbage token still gets an IP-scoped limit rather than being exempt |
| T-04-03-02 | Information Disclosure (Bearer token value in logs) | rate_limiter.py | low | mitigate | No logging call references the extracted token — Task 2's source-scan prohibition test |
| T-04-03-03 | Improper Input Validation | config.py rate_limit_per_key | low | mitigate | Validated via limits.parse_many at Settings construction, failing fast before any request is served, never deferred to request time |

</threat_model>

<verification>
Full suite: `.venv/bin/python -m pytest tests/ -v` must stay green.
</verification>

<success_criteria>
- Per-key limit enforced independently alongside the existing per-IP limit, before any provider call.
- RATE_LIMIT_PER_KEY configurable and validated at startup.
- All OBSV-02 edge-case and prohibition items resolved with passing tests.
</success_criteria>

<output>
Create `.planning/phases/04-observability-resilience/04-03-SUMMARY.md` when done.

## Artifacts Produced
- `config.py` — rate_limit_per_key field + _validate_rate_limit_per_key validator
- `rate_limiter.py` — extract_bearer_key(request) key_func
- `routes/chat.py` — stacked per-key @limiter.limit() decorator
- `.env.example` — RATE_LIMIT_PER_KEY row
- `tests/test_rate_limiting.py` (new), `tests/test_startup_validation.py` — OBSV-02 coverage
</output>
