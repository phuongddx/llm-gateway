---
phase: 04-observability-resilience
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - docker-compose.yml
  - prometheus/prometheus.yml
  - README.md
autonomous: true
requirements: [OBSV-01]

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "An operator can run docker compose --profile observability up and get a Prometheus instance scraping this gateway's /metrics"
    - "README.md accurately documents /health/live, /health/ready, /metrics, RATE_LIMIT_PER_KEY, and the tenacity dependency — no stale references to the old single /health endpoint remain"
  artifacts:
    - path: "docker-compose.yml"
      provides: "optional prometheus observability Compose profile service"
      contains: "observability"
    - path: "prometheus/prometheus.yml"
      provides: "scrape config targeting the gateway's /metrics"
      contains: "/metrics"
  key_links:
    - from: "prometheus/prometheus.yml"
      to: "main.py"
      via: "scrape_configs job target gateway:8000, metrics_path /metrics"
      pattern: "/metrics"
  edge_cases: []
  prohibitions: []
---

<objective>
Complete OBSV-01's Compose-profile success criterion and bring the operator-facing docs in line with everything Wave 1/2 shipped: the optional Prometheus sidecar that scrapes `/metrics`, and README's description of the health split, metrics endpoint, per-key rate limit, and the new `tenacity` dependency.

Purpose: this phase's code changes are only complete once an operator can actually stand up the observability stack described in ROADMAP Phase 4's success criteria, and once the docs stop describing the pre-Phase-4 single `/health` endpoint and per-IP-only rate limiting.
Output: `docker-compose.yml`'s new `prometheus` profile service, `prometheus/prometheus.yml` (new), README.md updates.

Depends on 04-01/04-02/04-03 landing first so the documentation and Compose scrape target describe the actual shipped behavior rather than anticipated behavior.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-observability-resilience/04-CONTEXT.md
@.planning/phases/04-observability-resilience/04-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Optional Prometheus Compose profile scraping /metrics</name>
  <precondition>docker CLI and daemon are available (established in Phase 3 — `docker compose version` succeeds).</precondition>
  <read_first>docker-compose.yml (full file, 14 lines), 04-RESEARCH.md's Environment Availability table and Recommended Project Structure section (prometheus/prometheus.yml scrape config, optional Compose profile)</read_first>
  <files>docker-compose.yml, prometheus/prometheus.yml</files>
  <action>
    Create `prometheus/prometheus.yml`: a minimal scrape config with one job (e.g. `job_name: llm-gateway`) targeting `gateway:8000` (the existing `gateway` service's Compose-network hostname) with `metrics_path: /metrics` and a short scrape interval (e.g. 15s).

    In `docker-compose.yml`: add a new top-level service (e.g. `prometheus`) alongside the existing `gateway` service, gated behind `profiles: ["observability"]` so it never starts on a bare `docker compose up`; use a specific, non-`latest` `prom/prometheus` image tag (verify the current stable release tag at implementation time — do not pin `latest`, per the repo's general reproducibility preference); mount the new `prometheus/prometheus.yml` read-only into the container at Prometheus's expected config path; expose port 9090. No changes are needed to the existing `gateway` service — Compose's default network already resolves the `gateway` hostname for the sidecar.
  </action>
  <verify>
    <automated>docker compose --profile observability config -q</automated>
    <fails_when>the command exits non-zero (invalid YAML, an unresolvable image tag, or a missing/misreferenced prometheus/prometheus.yml mount path).</fails_when>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml defines a profiles: ["observability"]-gated prometheus service using a pinned (non-latest) image tag.
    - prometheus/prometheus.yml scrapes gateway:8000/metrics.
    - The default (no --profile) `docker compose config` output is unchanged for the existing gateway service.
  </acceptance_criteria>
  <done>docker compose --profile observability config -q exits 0; the gateway service definition is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: README — health split, /metrics, RATE_LIMIT_PER_KEY, tenacity</name>
  <read_first>README.md:165-200 (GET /health section + env var table), README.md's dependency/requirements prose (if present) or nearest section listing runtime dependencies</read_first>
  <files>README.md</files>
  <action>
    Replace the `### GET /health` section's single `curl http://localhost:8000/health` example with two subsections documenting `GET /health/live` (process-up check, no dependencies, always 200) and `GET /health/ready` (200 only once the analytics DB and writer have initialized during lifespan startup, 503 otherwise) — including a one-line note that the old single `/health` endpoint was removed with no back-compat alias.

    Add a new `### GET /metrics` section documenting the unauthenticated Prometheus-format endpoint: the `gateway_requests_total{provider,model,status}` counter and `gateway_request_duration_seconds{provider,model}` histogram it exposes, that error rate is derived via PromQL rather than a separate metric, and that `docker compose --profile observability up` starts an optional Prometheus sidecar (per `prometheus/prometheus.yml`) that scrapes it.

    Add a `RATE_LIMIT_PER_KEY` row to the existing env-var table directly below the `RATE_LIMIT` row (default `60/minute`, per-Bearer-token, both limits apply independently).

    In the section describing runtime dependencies (or the nearest prose naming `requirements.txt`'s contents), add one line noting `tenacity` was added for same-provider transient retry with exponential backoff — an explicit, user-approved deviation from the otherwise minimal-dependency default.
  </action>
  <verify>
    <automated>grep -c '/health/live' README.md; grep -c '/health/ready' README.md; grep -c '/metrics' README.md; grep -c 'RATE_LIMIT_PER_KEY' README.md; grep -c 'tenacity' README.md</automated>
    <fails_when>any of the five grep counts is 0 (a shipped behavior — the health split, the metrics endpoint, the per-key rate limit, or the tenacity dependency — is undocumented).</fails_when>
  </verify>
  <acceptance_criteria>
    - README.md's GET /health section is replaced by /health/live + /health/ready documentation.
    - README.md documents GET /metrics and the optional Prometheus Compose profile.
    - README.md's env var table includes RATE_LIMIT_PER_KEY.
    - README.md notes the tenacity dependency addition.
  </acceptance_criteria>
  <done>All five grep checks pass with count >= 1; no remaining README reference to the old single /health endpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Optional Prometheus sidecar ↔ gateway | Internal Compose-network-only traffic; the sidecar is opt-in via a named profile, never started by default |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04-01 | Tampering (unpinned base image silently replaced by an unreviewed upstream release) | docker-compose.yml prom/prometheus image | medium | mitigate | Pin a specific, non-latest prom/prometheus image tag so the sidecar image is reproducible and not silently replaced |

</threat_model>

<verification>
`docker compose --profile observability config -q` exits 0; the five README grep checks in Task 2 all pass.
</verification>

<success_criteria>
- Optional Prometheus Compose profile validates and scrapes the gateway's /metrics.
- README.md fully documents the phase's shipped surface with no stale /health references.
</success_criteria>

<output>
Create `.planning/phases/04-observability-resilience/04-04-SUMMARY.md` when done.

## Artifacts Produced
- `docker-compose.yml` — optional `prometheus` service gated behind the `observability` profile
- `prometheus/prometheus.yml` (new) — scrape config for the gateway's /metrics
- `README.md` — health split, /metrics, RATE_LIMIT_PER_KEY, tenacity documentation
</output>
