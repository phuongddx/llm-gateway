---
phase: 03-containerized-deployment-ci
plan: 2
type: execute
wave: 2
depends_on: ['03-01']
files_modified:
  - main.py
  - analytics/__init__.py
  - .github/workflows/ci.yml
autonomous: false
requirements:
  - DEPL-03
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'TR1 (DEPL-03 pipeline shape, locked decisions): .github/workflows/ci.yml triggers on push AND pull_request targeting main and runs three jobs — lint (checkout + setup-python on 3.12, a CI-only ruff install, ruff check .), test (matrix python 3.12 AND 3.14 with fail-fast disabled and the pip cache, pip install -r requirements.txt, pytest -q), and docker-build (checkout + a plain docker build tagged llm-gateway:ci — no registry credentials, no third-party build/push actions).'
    - 'TR2 (green day one — Pitfall 7 ordering): ruff check . exits 0 on the post-fix tree under BOTH the local pre-existing 0.8.4 and the CI-pinned current version in a parity venv — all four known findings are gone (the F821 landed with 03-01; the two dead re-exports and the unused fastapi name land here); the pipeline never starts permanently red, which is the entire point of DEPL-03.'
    - 'TR3 (DEPL-03 end-to-end, concurrency edge disposed — probe category concurrency, "If interrupted or run in parallel, what is guaranteed?" per /tmp/gsd-edge-coverage-p3.json): after the phase commits push to origin main, the triggered Actions run completes with all three jobs green and the run URL is recorded in the SUMMARY; the 3.12 matrix leg IS the permanent regression test for the annotation blocker — a reintroduced undefined annotation name turns it red at collection (exit 2) before the change reaches any deployed container; fail-fast disabled guarantees BOTH legs report even when one fails; duplicate push/PR runs of one commit are harmless (idempotent checks, no deploy side); requiring the checks on main is a manual Settings->Branches operator step, flagged out of scope, not silently assumed done.'
    - 'TR4 (code hygiene boundary): main.py changes only its fastapi import line (dropping the one unused imported name — nothing else in the file references it); analytics/__init__.py becomes a 0-byte package marker matching the tracked empty routes/__init__.py convention; the full suite stays green on both regimes (/tmp/gw312 rig and .venv); requirements.txt is untouched.'
    - 'TR5 (scope boundary): no local lint Makefile target is added and no lint configuration file is created — the repo has none by documented posture (AGENTS.md) and the locked decision puts linting in CI only; a local target or config would be scope beyond the decision.'
  artifacts:
    - .github/workflows/ci.yml
    - main.py
    - analytics/__init__.py
  key_links:
    - 'ci.yml push/PR trigger -> lint job ruff check . <-> green-day-one requires the 03-01 F821 fix PLUS this plan''s three F401 fixes (the lint-green-before-lint-job ordering constraint from 03-PATTERNS Shared Patterns)'
    - 'ci.yml test matrix 3.12 leg -> providers package import -> the collections.abc AsyncGenerator import landed by 03-01 (the leg is that fix''s permanent regression test — the exact failure mode reproduced locally as exit 2)'
    - 'ci.yml docker-build job -> the 03-01 Dockerfile (the build fails fast if the file, .dockerignore, or any allowlisted module is missing — build proof rides CI where no local daemon guarantee exists)'
    - 'checkpoint (ruff SUS legitimacy verdict) -> the lint job install line (pin decision) — the package-legitimacy gate strictly precedes adoption; never auto-approvable'
  prohibitions:
    - requirement_id: DEPL-03
      category: transparency
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: 'MUST NOT push images or hold registry credentials — the CI docker job is build-only smoke per the locked decision; no third-party build/push actions enter the workflow; CI never silently becomes a deploy pipeline (specless-probe kept prohibition, descriptor-less / flagged-unverified).'
    - requirement_id: DEPL-03
      category: values
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: 'MUST NOT add ruff or any lint tooling to requirements.txt — runtime dependencies stay exactly the current documented set (REQ-NFR-02 baseline discipline keeps the container image slim); ruff exists only inside the CI workflow file as a CI-only dependency (specless-probe kept prohibition, descriptor-less / flagged-unverified).'
---

<objective>
DEPL-03: GitHub Actions gates every change — lint + tests on both Python regimes (3.12 and 3.14) + an image build, on push and PR to main, provably green on the phase's own commits.

Implements the locked CONTEXT decision "GitHub Actions CI" (ci.yml on push + PR to main; matrix 3.12/3.14 running pip install -r requirements.txt then pytest -q; one plain docker build smoke job with no push and no registry credentials) and the locked post-research decision (a tiny ruff lint job, ruff CI-only — never in requirements.txt — and the 4 existing findings fixed: the F821 AsyncGenerator crasher landed with 03-01; the 3 dead symbols land here). The decisions are LOCKED — this plan implements them exactly via the research-verified workflow skeleton.

Depends on 03-01 (wave 2): the 3.12 test leg requires the import fix and the docker-build job requires the Dockerfile; this plan adds no container files.

Purpose: a red pipeline on any future push identifies a broken change before it reaches the deployed container — the 3.12 leg is the permanent regression test for the phase's blocker, and the lint job must be born green (Pitfall 7) or CI starts as noise instead of a gate.

Output: the workflow file, the lint-green code fixes, a blocking human checkpoint recording the ruff adoption decision, and a green Actions run on origin main recorded as evidence.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-containerized-deployment-ci/03-CONTEXT.md
@.planning/phases/03-containerized-deployment-ci/03-RESEARCH.md
@.planning/phases/03-containerized-deployment-ci/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lint-green code fixes — the three remaining ruff findings (two dead re-exports, one unused fastapi import)</name>
  <files>main.py, analytics/__init__.py</files>
  <read_first>
  - .planning/phases/03-containerized-deployment-ci/03-RESEARCH.md — §Pitfall 7 (the four findings quoted verbatim with file:line:col and the do-not-add-a-red-lint-job warning), §Package Legitimacy Audit (ruff context)
  - .planning/phases/03-containerized-deployment-ci/03-PATTERNS.md — sections "main.py" (the one-line fix and the must-not-modify note on the lifespan), "analytics/__init__.py" (the 0-byte convention rationale and the rejected __all__ alternative), and "Shared Patterns" (lint-green-before-lint-job)
  - main.py lines 1-15 (the import block; the fastapi import at line 6)
  - analytics/__init__.py (both lines)
  - routes/__init__.py (the tracked 0-byte package-marker convention analog)
  - AGENTS.md — Code Conventions (Imports bullet)
  </read_first>
  <action>
  1. main.py — exactly one line changes: the fastapi import at line 6 currently imports two names, one of which is unused everywhere in the file (ruff F401, auto-fixable, verified by the research run); reduce it to import only the app-class name. Nothing else in main.py changes — the lifespan, static mounts, and router wiring are exactly what the 03-01 container exercises and must not be touched.

  2. analytics/__init__.py — remove both re-export lines (the DB class and the cost function; F401 twice) leaving the file 0 bytes, and KEEP the file: it is the package marker, matching the tracked empty routes/__init__.py convention. Clean-cutover rationale (03-PATTERNS): zero in-repo consumers were verified by repo grep — main.py imports from the concrete submodules — and keeping the re-exports behind an exports list would be dead code failing the delete-weightless-code bar.

  3. Prove green under the local pre-existing ruff (0.8.4 on the workstation): ruff check . exits 0 — with the F821 already fixed by 03-01, zero findings remain across all four known locations. Then the full suite on both regimes (the 3.12 rig at /tmp/gw312 — recreate per the 03-01 SUMMARY note if gone — and the 3.14 .venv): both exit 0.

  Commit per AGENTS.md conventions (chore or fix scope, phase-plan tag 03-02).
  </action>
  <verify>
    <automated>ruff check . && .venv/bin/python -m pytest tests/ -q && /tmp/gw312/bin/python -m pytest tests/ -q</automated>
    <fails_when>any ruff finding is reported (any of the four known file:line:col findings resurfacing, or anything new), the 3.14 suite exits non-zero (a real regression from the import change), or the 3.12 rig exits 2 with a collection NameError (the 03-01 fix regressed)</fails_when>
  </verify>
  <acceptance_criteria>
  - ruff check . exits 0 with no output.
  - grep -c "^from fastapi import FastAPI$" main.py returns 1; grep -c "import FastAPI, Request" main.py returns 0; the task's own commit diff for main.py shows exactly 1 insertion and 1 deletion.
  - analytics/__init__.py exists and wc -c analytics/__init__.py prints 0.
  - requirements.txt is untouched (empty diff for the file at commit time) — prohibition: ruff never enters runtime deps.
  - Both suites exit 0 (.venv and /tmp/gw312).
  </acceptance_criteria>
  <done>The tree is lint-clean under the default ruff rule set and both Python regimes pass — the CI lint job can be born green, satisfying the Pitfall-7 ordering constraint.</done>
  <reversibility rating="reversible">Two dead-code removals (one import name, two re-export lines); each revertible in isolation with no behavior change — the removed symbols had zero in-repo consumers (verified).</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: ruff adoption gate — SUS package-legitimacy verdict requires human verification before the CI lint job installs it</name>
  <what-built>Nothing yet — this is the package-legitimacy checkpoint mandated by 03-RESEARCH §Package Legitimacy Audit before the lint job adopts ruff. The gate tool returned verdict SUS with reasons "too-new" and "unknown-downloads"; RESEARCH verified the signals are weak (the too-new reason reflects the latest-release timestamp of a weekly release cadence, not package age — first release 2022-11-04; the unknown-downloads reason is a tooling data gap, PyPI download counts unavailable). Per protocol the SUS disposition stands: one human verification before adoption, never auto-approvable.</what-built>
  <how-to-verify>
  1. Open https://pypi.org/project/ruff (optionally also https://github.com/astral-sh/ruff) and confirm the counterevidence RESEARCH verified this session: maintainer Astral Software Inc., owners crmarsh/zanie/sarasklenka (Astral staff), Development Status 5 — Production/Stable, first release 2022-11-04, current version 0.16.6, and the adopter list including FastAPI, pytest, Pydantic, pandas, scipy.
  2. Approve ONE:
     (a) RECOMMENDED — adopt ruff pinned to the exact current version in the CI install line (RESEARCH A5: a future ruff release adding rules can never redden CI unexpectedly; the supply surface becomes a fixed artifact). This is the default if you approve without a preference.
     (b) Adopt unpinned latest (fresh rules on every run; occasional surprise-red risk documented in A5).
     (c) Reject the lint job — falls back to the CONTEXT-minimal CI (matrix + docker-build only); note this leaves DEPL-03's literal "lint + tests" unsatisfied, so it needs an explicit scope acknowledgment.
  3. The decision is recorded verbatim in the 03-02 SUMMARY and Task 3's install line implements it.
  </how-to-verify>
  <resume-signal>Reply with the chosen option — "approved pinned" (default), "approved unpinned", or "reject lint job" plus any conditions</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Author ci.yml (lint + matrix tests + docker-build), pre-push parity, push to main, green run</name>
  <files>.github/workflows/ci.yml</files>
  <precondition>Task 2's checkpoint approved ruff adoption with the pin decision recorded (default: pinned to the exact current version 0.16.6); Task 1's lint-green fixes are committed.</precondition>
  <read_first>
  - .planning/phases/03-containerized-deployment-ci/03-CONTEXT.md — the locked "GitHub Actions CI" decision block and the Post-Research Decisions paragraph (ruff CI-only, findings fixed)
  - .planning/phases/03-containerized-deployment-ci/03-RESEARCH.md — §Code Examples (the ci.yml skeleton with the pip-cache caveat note), §State of the Art (action majors verified current 2026-07-20), §Alternatives (plain docker build over third-party build actions), §Validation Architecture (pytest exit-code semantics 0/1/2 and the requirements-to-test map)
  - .planning/phases/03-containerized-deployment-ci/03-PATTERNS.md — section ".github/workflows/ci.yml" (the skeleton quoted verbatim, the hard ordering constraint, and the checkpoint note), "No Analog Found" (the .github directory is verified absent — create it)
  - AGENTS.md — Runtime/Tooling Preferences (no linter configured locally is the documented posture; CI-only linting does not change it)
  </read_first>
  <action>
  1. Author .github/workflows/ci.yml (new directory) from the 03-PATTERNS skeleton verbatim: workflow name CI; triggers on push and pull_request, both scoped to the main branch; job lint on ubuntu-latest — the official checkout action at the current major, the official setup-python action at the current major with python-version 3.12, an install step for ruff implementing the Task 2 decision (pinned to the exact current version by default — replace the skeleton's floating install with the pinned form; if the checkpoint chose unpinned, keep the floating form), then ruff check .; job test on ubuntu-latest — strategy fail-fast disabled, matrix python-version exactly the strings 3.12 and 3.14, per-leg setup-python with the pip cache enabled, pip install -r requirements.txt, pytest -q; job docker-build on ubuntu-latest — checkout then a single plain docker build tagged llm-gateway:ci (Docker is preinstalled on ubuntu runners; no setup step, no push, no credentials — prohibition). Drop the bracketed research annotations from comments; keep the operational ones. Nothing beyond the two official actions appears anywhere in the file.

  2. Pre-push parity (the CI lint command under the CI ruff version — guards the local-0.8.4 vs CI-version skew in the direction that matters): create a throwaway venv at /tmp/ruffpin, install the SAME pinned ruff version into it, run its ruff check . — exit 0 required before any push. Re-run both suite regimes once more (the exact commands the test job runs are pytest -q after installing requirements.txt — the /tmp/gw312 rig already proves the 3.12 leg; the docker-build job's command was proven live in 03-01).

  3. Commit, then push to origin main (remote git@github.com-phuongddx:phuongddx/llm-gateway.git — verified). The push itself triggers the workflow (push trigger). Watch the run to completion: gh is installed — gh run list --branch main --limit 1 then gh run watch with the run id; if gh is not authenticated, take the run URL from the push output or the Actions web UI and poll it there (an auth need at this point is a dynamic checkpoint, not a plan failure).

  4. Record in the SUMMARY: the run URL, the three job conclusions (lint, test 3.12, test 3.14, docker-build — four check rows across three jobs), the Task 2 decision verbatim, and the DEPL-03 statement — a red pipeline on any future push identifies the broken change before it reaches the deployed container. Note that requiring these checks on main (branch protection) is a manual Settings->Branches operator step, flagged to the user, out of scope here.
  </action>
  <verify>
    <automated>[ -x /tmp/ruffpin/bin/ruff ] || python3 -m venv /tmp/ruffpin; /tmp/ruffpin/bin/pip install -q ruff==0.16.6 && /tmp/ruffpin/bin/ruff check . && .venv/bin/python -m pytest tests/ -q && grep -c "^name: CI$" .github/workflows/ci.yml</automated>
    <fails_when>the pinned-ruff parity check reports any finding (version skew — fix before pushing; never push a known-red pipeline), the 3.14 suite exits non-zero, the workflow file is missing or misnamed (grep count not exactly 1 — the file must live at exactly .github/workflows/ci.yml), or the pushed Actions run shows any job failed or no run triggered at all (trigger or path typo — re-check the on: block and the file location)</fails_when>
  </verify>
  <acceptance_criteria>
  - .github/workflows/ci.yml exists at exactly that path; grep -c "^name: CI$" .github/workflows/ci.yml returns 1.
  - Triggers: grep -c "branches: \[main\]" .github/workflows/ci.yml returns 2 (push + pull_request).
  - Jobs: grep -cE "^  (lint|test|docker-build):$" .github/workflows/ci.yml returns 3; grep -c '"3.12", "3.14"' .github/workflows/ci.yml returns 1; fail-fast disabled is present (grep -c "fail-fast: false" returns 1).
  - Lint install implements the checkpoint decision — default pinned: grep -c "ruff==0.16.6" .github/workflows/ci.yml returns 1 (or the SUMMARY records the chosen alternative and the line matches it).
  - Supply surface: grep -c "uses: " .github/workflows/ci.yml returns 5 (checkout+setup-python in lint, the same pair in test, checkout alone in docker-build) and every uses line names one of the two official actions — no third-party action, no secrets block, no push step (prohibition).
  - Docker job: grep -c "docker build -t llm-gateway:ci" .github/workflows/ci.yml returns 1.
  - Parity: /tmp/ruffpin/bin/ruff check . exits 0; both local suites exit 0.
  - Green run: the SUMMARY records the Actions run URL with lint, test (3.12), test (3.14), and docker-build all concluded success; where gh is authenticated, gh run list --branch main --limit 1 shows the latest run conclusion success.
  </acceptance_criteria>
  <done>DEPL-03 is live and proven on the phase's own commits: every push and PR to main runs ruff lint, the full suite on both Python regimes, and an image build; the run went green end-to-end and is recorded as evidence.</done>
  <reversibility rating="reversible">One new workflow file (plus the two Task 1 dead-code removals already committed) — deleting the file restores the pre-CI state; merged-run history and the recorded green run remain as audit artifacts, not repo-config one-way doors.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → GitHub Actions runners | the workflow file and the code it checks execute on third-party infrastructure (ubuntu-latest) |
| CI install step → runner environment | ruff from PyPI crosses a supply boundary into the pipeline (the SUS-verdict package) |
| push event → main | anyone with repo push access triggers the pipeline; the pipeline itself gates nothing until branch protection is configured |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | ruff install in the lint job (SUS package-legitimacy verdict) | high | mitigate | Blocking human checkpoint (Task 2, gate blocking-human — never auto-approvable) verifying pypi.org/project/ruff BEFORE the workflow lands; pin the exact version in the install line so the supply surface is a fixed artifact (A5); ruff never enters requirements.txt (prohibition) |
| T-03-06 | Tampering | third-party actions supply chain (checkout, setup-python references) | medium | mitigate | Only the two official actions, referenced at the current majors verified this session (v7.0.1 / v7.0.0, 2026-07-20 per RESEARCH); the acceptance gate counts the uses lines at exactly 5 so no third-party action can slip in |
| T-03-07 | Tampering | unpinned >= requirements in CI (dependency drift between runs) | low | accept | Locked repo posture — no lockfile is the documented project choice; the pip-cache/restored-version caveat is documented in RESEARCH; matrix consistency within a run is what CI guarantees here |
| T-03-08 | Information Disclosure | workflow secrets surface | low | accept | The workflow defines no secrets and needs none (build-only, no registry) — the absence is intentional and recorded; nothing to disclose |

</threat_model>

<verification>
- Lint green under both versions: ruff check . (local 0.8.4) and /tmp/ruffpin/bin/ruff check . (CI-pinned version) both exit 0.
- Both regimes green: .venv (3.14) and /tmp/gw312 (3.12) full suites exit 0 — the 3.12 leg is the annotation-blocker regression test.
- Workflow shape: the Task 3 acceptance gates (name, dual triggers, three jobs, matrix strings, fail-fast, five official-action uses lines, build-only docker step) all pass.
- End-to-end: the pushed run on origin main is green across lint, test 3.12, test 3.14, and docker-build, with the URL recorded in the SUMMARY.
- Scope: requirements.txt, Makefile, and all container files from 03-01 are untouched by this plan; no local lint target or config appears.
</verification>

<success_criteria>
- DEPL-03: GitHub Actions runs lint + tests (and builds the image) on push and PR to main — proven by a green run on the phase's own commits, with the run URL as evidence.
- The lint job was born green (all four findings fixed across 03-01 + this plan) and its supply chain was human-verified before adoption (blocking checkpoint, decision recorded).
- A hypothetical broken change — an undefined annotation name, a failing test, a broken Dockerfile — turns the pipeline red before it reaches the deployed container; branch protection is flagged as the remaining manual operator step.
</success_criteria>

## Artifacts this phase produces

Phase-union inventory (extends the 03-01 list; this union is authoritative):

- .github/workflows/ci.yml (NEW) — three jobs: lint (CI-only ruff per the recorded checkpoint decision, default pinned to the exact current version), test (matrix 3.12 + 3.14, fail-fast disabled, pip cache, pytest -q), docker-build (plain build tagged llm-gateway:ci, no push/credentials).
- main.py — the fastapi import line reduced to the used name only (dead-symbol fix).
- analytics/__init__.py — 0-byte package marker (dead re-exports removed, routes/__init__.py convention).
- From 03-01: providers/openai_compatible_base.py import fix, Dockerfile, .dockerignore, docker-compose.yml, Makefile docker targets, README Deployment (Docker) section.

<output>
Create `.planning/phases/03-containerized-deployment-ci/03-02-SUMMARY.md` when done
</output>
