apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  ANALYTICS_DB_PATH: /app/data/analytics.db
  ANALYTICS_QUEUE_SIZE: "1000"
  ANALYTICS_RETENTION_DAYS: "90"
  CORS_ORIGINS: https://stress-api.dropitx.site
  RATE_LIMIT: 60/minute
  RATE_LIMIT_PER_KEY: 60/minute
